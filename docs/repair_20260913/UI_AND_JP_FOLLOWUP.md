# 2026-09-13 UI / full JP follow-up

Baseline: `codex/takeover-20260913`, HEAD `96a659133a0339c68ffc86ae9caf0adf51d6c97f`. Existing unrelated generated UID files are preserved. Original checkout / user data / maps / equipment attribute master remain protected. Bootstrap reports only the legacy branch routing whitelist and missing branch rule; this continuing cross-system task retains its recorded integration baseline and isolated cache. The user's current single-controller instructions override the older checked-in delegation text. No engineering subagents are used.

## Current user decisions

- Repair shop first-open geometry. Restore buy/sell/repair/quantity controls from the user-designated desktop `HardCore-20260909-r3-closure-debug.apk` and calibration parameters. This supersedes an arbitrary 50-pixel relocation.
- The reference APK's embedded build info is clean HEAD `909821c928ddb66e5fd48f7cef718baa2e8bfe03`, version 72 / `1.21.0-r3-closure`. Its embedded calibration has buy `[47,318,270,51]`, repair `[47,381,270,51]`, sell `[44.7552490234375,381.399688720703,269.932434082031,51]`. Quantity row is defined by source at `[20,302,326,46]`; its children retain the profile-specific calibrated coordinates. Do not reintroduce the later item-presenter-driven action reflow.
- Keep 商品详情 above the independent item title. Buy/sell detail uses the same formatter as bag/warehouse. Remove repeated prices/high-value text, preserve actual quotes and confirmation transactions. Hide zero/absent item stats; retain 0–1 and 1–0. Long detail bodies remain clipped and touch-scrollable, with no visible scrollbar. Do not show internal notes or unlocalized schema keys.
- User emphasized latency across ALL UI: first opening, reopen, selection, switching, and mass operations. Preserve transaction durability and input correctness; measure actual paths.
- Full JP remains required. User chose existing formal sources first, explicitly routed lookup to **21cq's 1.76 data**, and clarified candidate DBs may be used only after proving the selected data are standard 1.76. No blanket approval to use the candidate DB merely because its folder says 176. Equipment base attributes/requirements/weight remain the user's master authority.
- After all work and checks, deliver a new APK on the desktop (v76 is the previous build; do not overwrite it).

## Findings / changes so far (not final acceptance)

- Shop separately formatted every non-consumable as equipment and injected price/risk copy. It now delegates to the common item formatter. Shared formatter filters only zero/missing stat ranges, and includes real use descriptions.
- `UIShopDetailSpace.region()` moved/resized buttons as a side effect of measuring selected item text. It is now read-only with respect to calibrated actions; 商品详情 remains visible.
- Detail overflow enabled scrolling after TouchScrollSupport's discovery pass, so long bodies could remain unregistered. The presenter now registers the body explicitly and hides the internal scrollbar through its visibility signal while retaining actual scroll state.
- `magic_defense_min/max` were absent from localized modifier labels, causing raw English keys in player-visible added stats. Added labels; unknown schema keys no longer fall through as display text.
- Selection styling removed/reinstalled identical theme overrides on every cell refresh. Idempotent resource comparison avoids the repeated descendant theme notifications (including avoiding an empty bulk-theme transaction).
- Icon refreshes deep-copied full catalog records, including artwork frame metadata. A narrow icon-path projection uses the same authoritative resolver. Public `get_item_record()` still returns a defensive deep copy. A separate rule snapshot excludes only artwork for pricing and saved-drop validation; all gameplay/identity fields are retained and checked for parity.

## Evidence in progress

- `outputs/test_logs/ui_latency_before.json`: real panel CPU baseline, 100 occupied bag slots / 500 warehouse in-memory records. Baseline medians: inventory refresh 43.875 ms, warehouse refresh 93.981 ms, warehouse hide/show 170.207 ms, shop sell switch 180.220 ms. These are headless CPU calls, not rendered/device timings or a real-storage bulk transfer benchmark.
- Latest measured intermediate medians before the pricing projection: inventory refresh 2.501 ms (P95 3.102), warehouse refresh 11.285 ms (P95 12.551), warehouse hide/show 21.750 ms; shop quotes still 71.285 ms. More work and new comparable timings are pending. Initial skill microbenchmark had empty entries; do not use it to claim production skill opening is validated. A proper `open_for()` measurement is required.
- Direct UI tests: `runner_results_adhoc_20260913_083853_066_24436.json`: `ui_detail_contract_test`, `inventory_real_input_test`, `selection_detach_smoke`: 3 PASS, no engine errors. Newer changes require relevant follow-up gates.
- `ui_latency_probe` is an instrumented headless diagnostic, not visual acceptance; it is force-tracked because the repository ignores names containing probe. Its output includes all actual catalog descriptions for copy inspection.
- Earlier single-heading / no-overflow tests now conflict with the user's explicit revised contract and must be updated narrowly while keeping text integrity, title clearance, clipping, transaction, and touch checks.

## JP candidate web evidence (not yet integrated)

- https://www.21cq.com/mir/View.aspx?id=181 : 辟邪手镯, agility +2; page identifies the catalog as 2003 1.76.
- https://www.21cq.com/mir/View.aspx?id=159 : 白色虎齿项链, magic evasion +20%.
- https://www.21cq.com/mir/Item.aspx?u=12 : 1.76 taoist necklace list, distinguishes evasion / accuracy / agility families.
- https://www.21cq.com/chuanqi/519.html : author's poison/evasion test and explanation of secondary lanes; identifies lucky/evasion necklaces as memory/lantern/white tiger tooth. Its statements about historical deletion need version-specific interpretation; not a probability table.
- https://www.21cq.com/chuanqi/551.html and /749.html : additional rare-attribute discussion; distinguish later versions rather than importing their equipment.

Full JP must use a new persisted affix version. Preserve v1 and already-delivered v2 deterministic validation/seeds; do not reinterpret v76 drops. Source primary original `M2Server/ItmUnit.pas` remains authority for distributions and byte semantics, subject to verified exact 1.76 item type bindings. Secondary attributes must have real gameplay consumers, not display-only fields.

## 09:55 continuation: measured storage work and async lifecycle

The in-memory panel benchmark did not represent real storage. A new isolated test fills 500 slots with valid deterministic drops and transfers 100 items. The first real-storage baseline was approximately 803 ms deposit / 765 ms withdraw, bank read 76.89 ms. Catalog projection, exact-ID drop validation, reusable positive validation, content-bound parsing and digest snapshots reduced synchronous transfer to roughly 319 / 341 ms, but this remained a visible stall.

A dedicated `warehouse_prepared_transaction.gd` now prepares three PRIVATE temp files (WAL, shared, profile) in WorkerThreadPool. It normalizes/encodes JSON, computes the unchanged original digest contract, flushes and reads back the bytes. It cannot promote authority or acknowledge inventory. PlayerState retains the unchanged original synchronous API for compatibility; the real WarehousePanel uses the new asynchronous preparation path. Main-thread checks validate normalized profile/shared records, dependencies, unique IDs, gold invariance and revision. Immediately before the no-await promotion phase it checks the profile/paths, save generation, original inventory/warehouse snapshots and exact current disk bytes. Any change cancels without applying inventory. Final promotions retain WAL, backups, exact readbacks, rollback and restart recovery. UI closure does not own the transaction lifetime. Focus loss/pause cancels preparation before saving.

Initial async benchmark: normal 100-item operations complete over 20–24 updating frames, ~188–236 ms wall time, max observed frame gap ~33–40 ms; the injected disk-failure rollback deliberately remains synchronous (~148 ms gap). These are CPU/headless observations, NOT device/GPU acceptance or a claim of zero dropped frames. A subsequent split of preparation stages and UI-destruction/external-file tests is currently being checked.

New regression found and fixed an existing malformed v2 `drop_affix` type comparison: non-Dictionary data must reject without script errors. Drop-validation cache now distinguishes full payload hashes so native-int and parsed-float snapshots do not constantly replace each other. All cached decisions compare complete private snapshots, not hashes alone. Collection memoization is restricted to one synchronous transaction; current file bytes and migration/profile dependencies remain checked.

Useful runner evidence:
- `runner_results_adhoc_20260913_093631_714_6892.json`: persistence snapshot cache, shared transactions, migrations, real storage latency — 4 PASS.
- `runner_results_adhoc_20260913_094750_754_20116.json`: async transaction success, overlapping request rejection, stale inventory cancellation, second-file failure rollback, failed rollback + original WAL restart recovery — PASS.
- `runner_results_adhoc_20260913_094912_596_5956.json`: async transaction, old synchronous transactions, warehouse UI, persistence cache — 4 PASS.
- Latest run log `outputs/test_logs/warehouse_async_lifecycle_runner.log` adds actual UI destruction and same-size valid external save overwrite; results pending at time of this note.

Still required: finish async lifecycle checks and all affected regressions, final UI measurements, full JP v3 implementation and behavioral tests, source review/commit, new verified desktop APK. No APK has been built from these follow-up changes.

JP interpretation correction: original random weapon btValue[7] is positive Source = weapon strong (强度); it is NOT holy damage. Original ObjBase consumes weapon strong by subtracting it from weapon wear. Recovery bytes exist and are accumulated in the original source, but the primary server suite has no actual HP/MP/poison recovery consumer. Auxiliary modern server code and 21cq community articles disagree on poison behavior; do not import a later rule and claim standard 1.76. The primary-first/version evidence decision remains open. 21cq's `/mir/Suit.aspx` is the standard 1.76 catalog; `/gonglue/608.html` and `/chuanqi/294.html` are explicit custom servers and are rejected as standard source evidence.

## 10:28 continuation: user decisions, completed drop audit, XP adjustment

- User explicitly excludes life recovery, spell recovery and poison recovery from JP pending verifiable 1.76 evidence. Do not add them. Weapon random strong is not holy. Original weapon speed bytes <=10 reduce speed (2/3 of the sign roll); >10 increase speed (1/3). Preserve raw draw order for rule reproduction.
- User requested every level threshold become 70% of its CURRENT value. PlayerState now applies round(previous rounded source/30 threshold * 0.70), minimum 1. Source XP tables and gained/saved XP remain unchanged. Level 1=2, level 22=7000, level 23=8167. Level-up, rollback, multi-kill settlement, death penalty and HUD checks passed. Old XP assertions updated to the new explicit contract. `runner_results_adhoc_20260913_101650_455_5948.json` has three XP regression PASS; HUD separately PASS in the first runner. The first XP test had a test-only unescaped percent formatting error (fixed), not a production XP bug.
- Full UI regression 12 PASS: `runner_results_adhoc_20260913_100312_277_15732.json`.
- Latest real-disk warehouse async planner copy-on-write/incremental weight optimization: `runner_results_adhoc_20260913_095936_359_6276.json` 3 PASS (async 500 valid drops/100-transfer, original transactions, inventory weight). Normal deposit max frame gap 18.454 ms / withdraw 19.264 ms, warm 17.864 / 18.667 ms. Wall times ~193-199 ms span 25 updating frames. Intentional disk-failure rollback remains synchronous (~141 ms); this is NOT device acceptance.
- User asked GLM to scan current drops. First preferred `codex exec --profile arkcli --model glm-5-3-flash-260901 --sandbox read-only` was blocked by CreateProcessWithLogonW:2 before reading any file. Stopped after repeated executor failure. Second run was explicitly INPUT-ONLY: supplied current structured table census, all 156 profile summaries, grouped 1289 elite/boss potion slots with UIDs, hashes, preservation checks, audit Python source and relevant production excerpts. GLM completed, provider/model confirmed by CLI. It did NOT independently read disk or execute tests. Raw candidate report: evidence/glm_drop_snapshot_candidates.md. The candidate's invented `assets/data/source_accounting/dpv2_21cq_source_accounting.json` path is REJECTED; actual source accounting is in dpv2_monster_drop_semantic_authority_v1.json. Its claim that 153/144 counts conflict is also rejected: those counts agree with current authority. All other candidate paths were checked in controller context.
- New read-only `tools/audit_current_drop_tables.py`: 156 profiles / 153 runtime allowed /144 enabled; 7611 unique slots +7611 effective records +7611 provenance,233 item classes,1289 elite/boss potion slots. Full exact-rational two-stage formulas, all manifest/raw source capture hashes, full legacy exception snapshots (IDs75/123/225), and frozen drop preservation compare PASS. No data changes. The audit initially produced false positives because disabled profiles have null IDs, AUTO_BOOST is the combined policy enum, and retained exception provenance lacks newer reward fields; corrected scanner schema to the verified production contracts and compared entire exception slots to original migration Git source.
- `tests/dpv2_21cq_direct_runtime_test.gd` had stale pre-V505 6809 counts, source profile counts/gold226=3000 and cap9. Verified current frozen 21CQ capture migration authorizes 7611, ID226 gold1500, and current counts; updated ONLY this stale test. Old inactive seed cap9 and semantic pre-migration accounting are intentionally untouched. Runtime production reads direct_baseline cap15.
- Expanded `tests/test_dpv2_repair_v5.gd` runs ALL7611 real slots through audit and optimized service with identical seeds, verifies RNG state and retained item/gold parity, solar-water existing denominator x2, cap15/accounting, and all-success six armor-boss retention. Both current full runtime tests PASS: `runner_results_adhoc_20260913_102553_869_17344.json`, zero engine errors. Existing single-player boost, loader, death budget queue also PASS in 101650 runner. Old failing assertions were test debt, not altered probability tables.

Outstanding: full JP v3 mapping/implementation/behavior proof; possible synchronous warehouse gold-transfer latency; final UI/performance/diff review, relevant remaining gates, commit exact source and build NEW desktop APK. Do not claim task complete or redeliver v76 as the new build.

## 11:01 continuation: pushed checkpoint and actual GLM remote scan

Checkpoint `191958eaa194639b1072149cd23e34a389d8a7e4` was pushed to origin `codex/takeover-20260913`; ls-remote matches. UI/XP/storage changes and their evidence are committed. No new APK yet.

GLM's native shell was unavailable and web search could not read fixed GitHub blobs. Added a tightly allowlisted read-only MCP bridge, `tools/remote_drop_scan_mcp.py`, invocation-only configuration, no new credentials/providers or persistent host settings. It reads public watermarkpp/HardCore at exactly 191958e, verifies the reviewed auditor against its remote source bytes, and executes deterministic mechanical checks using remote file readers. It never modifies project files or runs shell commands. An initial HTTPS truncation/SSL EOF was diagnosed; incomplete responses now reject before caching, with bounded read retries.

The raw Git audit found 144 hash representation mismatches: the logical source and 143 captured HTML files have LF Git blobs but historically recorded CRLF raw hashes. Every full recorded hash matches after the reversible CRLF restoration (no content edits). The bridge reports both hashes and all restorations explicitly; this is not a waiver of unmatched content. Added explicit CRLF attributes for the captured HTML `.bin` files to preserve these historical bytes in future checkout/build environments, instead of depending on Windows core.autocrlf. Existing checkout source bytes and every probability table remain unchanged.

GLM session `01a098b2-ee17-7ad1-8249-4c13963db504` completed through one actual MCP resource read: 174 remote files, all 7611 slots/effective/provenance records, 143 source captures; zero final errors, five frozen comparisons equal except already-authorized cap/binding fields. Its final candidate report is `evidence/glm_remote_drop_candidates_191958e.md`; complete controller-computed remote evidence is `evidence/remote_drop_audit_191958e.json`. Full static exact-rational/hash/preservation PASS; Godot runtime parity was separately verified in 102553 runner. GLM did not execute Godot, test performance or accept the project. Remaining JP and new APK work is unchanged.

## Final source implementation and acceptance scope

This section supersedes the earlier intermediate status entries. Source baseline for this continuation is pushed commit `191958eaa194639b1072149cd23e34a389d8a7e4`. The new APK must identify the subsequent committed source SHA, rather than claiming the earlier v76 APK contains these changes.

- JP v3 is integrated for all 175 master equipment IDs. The generator and [complete equipment whitelist](JP_EQUIPMENT_RULES.md) share the same authority. Original independent multi-point distributions, accuracy, agility, signed weapon speed, weapon strength, eligible necklace luck/evasion, eligible ring poison avoidance and random maximum durability are implemented. The outer entry gate is 1/2 instead of 1/10; this is a fivefold single-player adjustment, not a claim that half of drops gain a combat attribute. Three disputed recovery attributes remain excluded. Existing v1/v2 instances keep their original deterministic interpretation; extension equipment outside the 175-ID master still drops normally without borrowing a JP type.
- Source priority: original primary Pascal item generation and runtime byte interpretation own the rules. Only 100 selected ordinary accessory type fields are taken from the user-routed candidate after checking their full primary attribute signature; the custom database as a whole is rejected. Generated JSON records exact missing-field inventories, distribution, hashes, selected row IDs and the two explicit user-master requirement differences. No primary equipment attribute is replaced. Startup preloads and validates the immutable JP catalog so the first drop cannot trigger its JSON load.
- Strength reduces weapon durability consumption. It does not increase damage or maximum durability; random durability is a separate roll. Primary wear code is the implementation authority. Contemporary corroborating player reports include [2003-07-24](https://mir.17173.com/content/2003-7-24/n287_203362.html); later speculative hidden-attribute claims are not imported.
- Equipment speed applies the melee interval ratio to wizard and taoist spell actions, release timing, locks and recast timing. At +1, the ratio is 840/900; a 600 ms action becomes 560 ms and a 1500 ms recast becomes 1400 ms. Actual fireball and talisman launch events were compared in concurrently advanced frames. Warrior special-skill cooldown contracts remain unchanged.
- Player ranged/magic avoidance is decided once per incoming hit before damage and attached effects. It includes physical projectiles, adjacent projectile launches, mixed physical/magic releases and all harmful magic, including monster area magic. Ordinary melee accuracy/agility and monster-target spell eligibility remain separate. Poison avoidance follows the original monster poison-control consumer; it is not silently redefined as damage reduction for every poison tick.
- Attribute names share one localized link/declaration helper in inventory, warehouse and shop detail presenters. Same-link click/touch closes the explanation. New content, detail closure, UI closure, ancestor visibility changes, scrolling and UI destruction all dismiss it. The help canvas is owned by the presenter; no persistent global overlay is retained. Links are local whitelisted attributes only.
- The real input bug found during helper testing was that the existing blank-tap selection guard treated the RichTextLabel as an empty region. The two detail presenters now declare that label a protected functional input region. Mouse and native screen-touch tests pass through actual Godot GUI routing. The native-event fixture converts logical viewport coordinates through the viewport transform; it does not patch production input ordering.
- Bank gold operations now use the existing prepared-WAL transaction worker and main-thread authority/promotion gate. The view reads one validated balance/sequence snapshot and does no disk authority reads on submission. The service revalidates every request; stale displayed sequences reject without automatic replay. Closing/freeing the UI does not lose an owned transaction. Duplicate rejection, overlap rejection, external state changes, individual write failures, rollback failure and original restart recovery are tested.

### Final verification evidence

The tracked `evidence/followup_verification.json` indexes completed runner reports, immutable source hashes and preservation checks. These runs cover the patch tree before committing; final committed-source smoke and APK identity are recorded separately with the delivered artifact.

- Feature gate `runner_results_adhoc_20260913_121608_672_3876.json`: five PASS, zero engine errors — JP v3, actual mouse/native touch help lifecycle, actual spell speed, ranged/magic evasion and prepared bank UI/transactions.
- JP test includes 24,000 weapon samples plus 22,400 all-ID samples; every generated modifier is checked against an independently stated per-type whitelist, with JSON persistence, forged-value rejection and real combat/wear consumers.
- Original v2 distribution, item persistence/durability/blessing, caster timing, direct spells, mixed-damage atomicity, monster accuracy, exact ranged delivery, warehouse recovery and input-dismissal regressions passed. Tests that isolated geometry or AC/MAC now explicitly set evasion to zero; the new evasion test covers hit/miss behavior separately. The former assertion that weapon speed must never affect spells was updated to the user's new requirement. No production requirement was weakened to obtain PASS.
- Actual bank-button baseline: synchronous submission 80.329 ms, maximum observed frame gap 81.641 ms. After view snapshot repair, the same 100-inventory/400-warehouse scenario measured submission 5.573 ms and maximum gap 25.105 ms. Normal service submission is about 0.03–0.06 ms; completion spans approximately 28 updating frames. See the committed timing JSON for the latest repeat. Injected disk-failure rollback still has a synchronous 120–150 ms recovery path; this is distinct from normal gameplay latency.
- Inventory/warehouse/shop/skill repeat-open and update timings, 100-item storage transfer timings and bank timings are preserved with this follow-up. Constructor cost is paid by the existing startup prewarm before gameplay. Measurements are desktop headless CPU evidence, not GPU measurements or Android frame-rate acceptance.
- Maps, manual map workspace, canonical monster catalog, user equipment master, UI artwork, manual calibration and rarity mapping remain byte/content identical to their protected baseline. Drop probabilities and priority tables remain unchanged after the verified 191958e remote scan.
- Android device verification: NOT_RUN. ADB returned no connected devices. APK export, signature/resource/source-identity verification and desktop delivery follow the final source gate; successful export cannot substitute for device acceptance.
