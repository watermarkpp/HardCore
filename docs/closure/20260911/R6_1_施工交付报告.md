# HardCore R6.1 全量回归修正施工交付报告

日期：2026-09-11。执行者：GLM5.3flash（受控执行：安装、接线验证、回归、证据整理）。核心设计/代码/最终验收：主控。
施工包：`HardCore_R6_1_Full_Repair.zip`（SHA256SUMS.json 校验通过，由安装器在 apply 前复核）。

## 1. 现场与身份

| 项 | 值 |
|---|---|
| 主树现场（开工记录） | 分支 `codex/m30-r3-integration`，HEAD `b3296528d7f85c7dff2c78adafaa0f5e4ec33c16`，dirty=0 |
| 固定基线 | `519dca099e0ac134f3e6e753ed27e2aeeb659e9f`（BASE，v74 发布态） |
| 施工工作树 | `C:\Users\Administrator\Documents\HardCore-worktrees\r6-1-full-repair`，新分支 `codex/r6-1-full-repair` @ BASE |
| 用户实测 APK（B 版本） | `HardCore-20260911-ui5-m30-closure-v74.apk`，461,894,798 字节，SHA-256 `5142BC1D38F99096556A76F611CEE3B7A9C176633AFD58842682708186063A9A` |
| 本轮提交 | `796d83af` apply(R6.1) 包原始字节（35 文件）；`9d4caaa8` test(R6.1) 独立测试补丁（2 文件，REVIEW_REQUIRED） |
| 安装器 | `--mode check` → `PREFLIGHT_PASS_NOT_APPLIED`（27 文件）；`--mode apply` → `APPLIED_NOT_COMMITTED`（27 文件）；回执 `outputs/r6_1/02_apply/receipt.json` |

未合并主树、未改正式版本号、未推送发行、未构建分发 APK（遵守执行单）。

## 2. 覆盖对照（执行单九项）

| ID | 要求 | 本轮落地 |
|---|---|---|
| UI-01 | 名称缺失 | 包内 presenter：空名回退、运行时文本/布局版本保护、显示恢复、真实标题高度；catalog 三域 46 物品全测（inventory/equipment/shop 各 PASS） |
| UI-02 | 一次点按重复效果 | `ui_activation_once.gd` 统一手势序号保护，接入装备槽/背包格/仓库格/商店卡；`activation_route_test` 13 项断言全过（含真实视口触摸路由、双指、拖动、取消、重复 DOWN） |
| UI-03 | 背包/仓库属性窗停靠、完整、无滑块 | 真实富文本测量+向下增高；`detail_layout_test`/`ui_r5_core_test` 全过；截图 01/05 实证 |
| UI-04 | 买卖详情不压二级框 | 用校准后 DetailPanelDecoration 内部开口；`detail_layout_test` shop 域 PASS；截图 02 实证（标题+两按钮净空） |
| UI-05 | 偏竖向合理宽高比 | 首选宽 220、上限 280、高≥1.12×宽；detail/name-style 测试 PASS |
| UI-06 | 音乐/音效各一行、短滑条、现有按钮框 | 每行 356×76（标签 92+滑条 152×56+百分比 50，复用 GothicSystemSettingsRowGemButton）；`system_menu_gothic_ui_test` PASS；截图 04 实证 |
| PERF-01 | 12/15 怪掉帧 | 空间桶查询合批 + 直达分支单次阻挡计算（不改碰撞/攻击规则）；`spatial_batch_test` 等价性 PASS；**12/15 真机帧数据 NOT_RUN（无设备）** |
| PERF-02 | 整体流畅性 A/B/C | 设备三版本矩阵 NOT_RUN（无设备）；桌面同机 headless 补充对照 BASE vs r6.1（4 场景 × 10/20/30 怪）无系统性回退（见 §5） |
| UI-07 | 稀有度名称颜色 | `ui_item_name_style.gd` 统一调色板（普通暖白/沃玛亮金 #FFD86B/祖玛暗金 #C6A04B/赤月橙 #FF943D/极稀有紫 #C48CFF）；`name_style_test` PASS；截图 01/02 实证沃玛亮金；配色 CSV `02_apply/rarity_mapping.csv` |

## 3. 测试结果（项目原 tools/run_godot_tests.ps1，全部真实运行）

- 包验收电池 9/9 PASS（`run_acceptance.ps1`）：name_style / detail_layout / activation_route / spatial_batch / catalog_inventory / catalog_equipment / catalog_shop / ui_r5_core / system_menu_gothic_ui；engine_log_errors=0。原始 JSON：`outputs/test_logs/runner_results_adhoc_20260911_095222_*.json`
- 相邻回归 24/24 PASS（三批）：空间索引 9（enemy_query、melee/aoe broadphase parity、absolute contract、projectile×4）；缓存/巡边/移动 10（retarget cache、streaming non-regression、loot order、safe zone、neighbor step、cadence×2、forced relocation、attack gate）；批量/性能 5（mass death batch、environment collision batch、combat runtime ownership、crowd performance、runtime density performance）。原始 JSON 在同目录。
- 桌面性能 harness（hc_monster_ai/performance_comparison_test）：BASE 与 r6.1 双侧 `HC_REV07_FULL_FRAME_PASS`。

## 4. 独立测试补丁（9d4caaa8，REVIEW_REQUIRED，交主控审查）

1. `tests/r6_1/activation_route_test.gd`：push_input 需窗口坐标；headless 窗口 0×0 → final_transform≈0.04 缩放，原始 canvas 点全部脱靶，13/13 不触发（真实设备窗口 1598×720 恒等变换不暴露）。最小复现：`tests/diag_activation_probe.gd`（PROBE via_push_pressed=0 vs via_parse_pressed=1，日志 `outputs/r6_1/diag_probe.log`）。修复=点先乘 `get_viewport().get_final_transform()`；断言零改动。
2. `tests/ui_r5_core_test.gd`：迁移后的有界断言仍对基础 region；新合同允许同侧纵向扩展（expanded_region），zoom=1.2 时 8 行正文需高 384>340，presenter 正确扩展（不压网格、scope 安全区内）。最小复现：`tests/diag_bounded_probe.gd`（日志 `outputs/r6_1/diag_bounded.log`）。修复=有界断言改用允许并集；"不压网格/不越左侧"等断言零改动。

无断言删除、无门槛放宽、无生产文件触碰。诊断夹具（diag_*）不入库，复现日志保留于 outputs/r6_1/。

## 5. 桌面性能补充对照（明确标注：非真机数据）

同机（本 Windows 工作站）、同场景 harness、headless。文件：`perf_a_base.stdout.log`（BASE=v74 内容）/`perf_c_r61.stdout.log`（r6.1）/对照表 `perf_desktop_base_vs_r61.csv`。

- p95（12 例）：8 例改善或持平（-20.5%~-1.2%），4 例小幅上升（+1.4%~+10.7%），最大波动 dense_crowd@30 -20.5%、sustained_close@30 +10.7%；headless CPU 噪声大，不据此宣称真机结论。
- 分项（enemy_metrics）：敌人 physics/movement/retarget/crowd/LOS/env-query usec 双侧同量级；`world_obstacles` 场景可见环境查询合批效果（environment_guard_batches 分桶）。
- 结论：无桌面可测回退信号；**PERF-01 12/15 怪与 PERF-02 三版本真机矩阵 = NOT_RUN（无设备）**，hc_r6_frame_probe.gd + analyze_frames.py 已随包就绪。

## 6. 审计

- `--mode audit` @ 796d83af（纯包字节）：**CORE_INTACT**，27 文件全部 INSTALLED，23 个冻结依赖全部 same_as_base=true（回执 `03_audit/receipt.json`）。
- `--mode audit` @ HEAD 9d4caaa8：按设计阻断，仅列 2 个已声明测试补丁文件（`04_audit_head_delta`），其余全部一致。
- 初次在临时 detached 树跑审计时因工作树锁损坏出现全量失配假象；重建干净树后消除，过程日志保留（`audit_full_output.txt`）。教训记录：审计树必须 `git -c core.autocrlf=false worktree add` 并含 outputs 回执。

## 7. 截图（真实面板、真实渲染，1598×720，`outputs/r6_1/screenshots/`）

01 背包+沃玛亮金「幽灵项链」停靠详情（无滑块、不压网格、竖向比例）；02 商店购买详情（亮金标题、不压二级框、按钮净空；货卡列表为空系夹具未注入玩法报价，真实货卡由 catalog_shop_test headless 覆盖）；03 系统菜单主页；04 音频设置页（音乐/音效各一行、短滑条、百分比、现有按钮框）；05 仓库（网格+转移+背包 6 件种子物品）。捕获夹具 `tests/diag_r6_screenshot_capture.gd`（未跟踪）。

## 8. 哈希与清单

- 46 个候选目标文件（含引擎 .uid）SHA-256：`outputs/r6_1/target_files_sha256.txt`；回执含每文件 expected/actual 双哈希。
- 配色 CSV：`outputs/r6_1/02_apply/rarity_mapping.csv`（UTF-8 BOM，canonical_item_id/canonical_name/source_tier/name_style/hex）。
- candidate.patch：`outputs/r6_1/02_apply/candidate.patch`（统一补丁，BASE→包期望）。

## 9. 未验证项与移交

- **设备（真机）验收全部 NOT_RUN**：12/15 怪帧数据、三版本 A/B/C 矩阵、热降频/冷加载分解、实机点按复现（UI-02 防御性修正的真机触发未在本环境重现）。
- 商店截图货卡空置为夹具报价缺失，非产品缺陷；真实报价流 UI 建议主控在真机验收时拍照。
- Python 离线自检 51/52 PASS + 1 项 symlink 特权环境受限（与 M30 收口同款；原始 XML 在包 evidence/ + 本地 `offline_tests_local_rerun.xml`）。
- 已知发布阻断（VALIDATION_STATUS）：普通物品 NO_SPACE/CONTENT_OVERFLOW/POST_LAYOUT_OVERFLOW 必须清零才可分发——catalog 三域当前 0 例。
- 工作树未跟踪杂项：`docs/bugfix24/**/*.translation`（引擎导入产物）与 `tests/m30_*/**.uid`（导入生成），均非包内容；audit_scope 会列出，属预期。
