# BICH-MAP-3：天然洞穴D011—D012验收报告

## 结论

运行地图248/249已从通用洞穴模板升级为客户端原MAP驱动的专用垂直切片。原始MAP证据、专用地块/洞壁资源、路线骨架、碰撞、灯光、怪物源坐标与门点安全区均已接入。

## 原MAP证据

| 运行地图 | 客户端MAP | 尺寸 | 阻挡率 | 光源单元 | 门单元 | SHA-256 |
|---|---|---:|---:|---:|---:|---|
| 248 洞穴一层 | D011 | 400×400 | 80.26% | 19 | 0 | `cfe306b793ec8b7cace580ee1efcf67e1dcbd4b98b4541855a5e070a1b33c067` |
| 249 洞穴二层 | D012 | 400×400 | 72.58% | 3 | 12 | `b6d301d103b3b6e87b1de46530913ca4c6b92c27f042bd469f356c4fb06147b2` |

两个MAP均符合52字节头部加每单元12字节的客户端结构。完整400×400可行走掩码保存在 `assets/art/maps/natural_cave/source_masks/`，没有用手机场景反向冒充原MAP。

## 运行接入

- 从两张MAP实际引用的 `Tiles.wil` 与 `Objects.wil` 图像构建512×32地块图集、768×128洞壁图集和独立洞穴微光。
- 248/249使用 `natural_cave` 专用资源集；最大可行走连通区压缩为手机战斗场的路线骨架，原始结构统计和哈希保持独立。
- 四个怪物刷新点均登记400×400源坐标；碰撞生成时主动避开刷新点、门点和路线。
- 248的两处门点与249的返回门点均保持畅通，两层往返和返回比奇闭环不变。
- D011/D012客户端包没有提供服务端MapInfo连接坐标，因此三个运行门点明确标为C级安全闭环候选；不宣称是官服精确坐标。

## 验证

- `natural_cave_source_integration_test`：哈希、尺寸、结构统计、专用资源、源坐标、碰撞、灯光与运行加载通过。
- `environment_template_test`：预热资源后重复切换，节点数量与内存增量稳定；避免把首次纹理驻留误报为泄漏。
- 怪物客户端资源和矿区/洞穴路线回归保持通过。本阶段不导出APK。
