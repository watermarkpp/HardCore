# 2026-09-06 八项升级施工记录

基线：`7c3019da4d3732766bdea2655abfedbea84e5b7b`，`codex/integration`，开工工作区干净。

## 范围与验收

1. 主城安全区 Loading 后 6 秒播放背景音乐；开始后自然播完，保持 70% 音量和单曲声部。
2. 用户 Desktop/sounds 的命名 NPC 音频随机互动播放，快速切换替换旧声，关闭互动不截断，与音乐独立。其他音效按同目录 SFX-CLOSURE-V1 方案进行来源、语义、接线、资源、回归核验；未证实映射不得猜配。
3. 修复召唤防御与已复现召唤系统缺陷；地图传送后宠物在玩家最终落点附近合法落位。
4. 第一册技能书获得基础 1 级，三册至基础 3 级；保留已有存档等级，验证失败不扣书。
5. 疾风药水图标不稳定：核对实际稳定身份和公共资源链。
6. 随机传送卷全图合法位置抽样，保留碰撞/边界/占位校验。
7. 远程/法术怪物按主源规则校正射程，保留 GU 与正式身份合同。
8. 用户显式几何覆盖：刺杀 3 GU、末端 1.5 GU 忽略防御；半月 2 GU、120 度。网络经典隔位说明与项目精确 GU 参数分别记录。
9. 用户施工中补充：升级经验再改为当前的三分之一，即原始主表的 1/30，沿用就近整数取整与最低 1；死亡经验扣除继续采用修改后的本级总门槛 10%。
10. 用户再次补充：全职业普通攻击、烈火射程统一 2 GU，烈火禁止空放；核查攻杀对所有普通攻击及技能的效果叠加，技能叠加不播放攻杀动画。
11. 用户补充尸王连续 10 只零掉落：核查原始概率、既有增幅、掷骰、落地生成全链，修复实现错误；保留人工爆率配置，不以重调倍率替代定位。

交付：实现、必要回归、远端同步、新完整 APK 及桌面副本和哈希。手机听感与实际玩法验收独立记录，未取得时不得声称通过。

## 所有权

- 主控：GameRoot / PlayerState / SkillProgressionService 与跨系统接入、测试入口、提交构建验收。
- audio_source_rework（Sol）：音频主源复核、服务、素材/映射/工具/专项；替换初始 audio_0906 草案，不沿用未经核验的猜配。后续独占音频观察接口及缓存生命周期小段。
- summon_skills_0906：SummonActor、道士/战士数学、战士技能合同和专项。
- monster_item_0906：EnemyActor 射程及物品图标公共加载链和专项。
- 三个 worker 在当前基线互斥写区执行，Godot 缓存由主控串行授权；禁止共同写缓存或 Git 暂存。地图数据、已批准升级特效、美术像素、怪物数量和已验收 AI 节拍冻结。

## 初步证据

- 音乐 TownMusicController 原先在 `set_town_presence(false)` 调用 stop，离开安全区会截断。
- 原随机传送 `_find_valid_random_teleport_position` 仅围绕玩家抽样 3..16.25 GU；主源 `dev_art_sources/reference/original_gameofmir/M2Server/ObjBase.pas` 的 `TBaseObject.MapRandomMove` 使用地图 Width/Height 抽样。本次采用正式可玩地图完整范围，并通过现有实际碰撞过滤。
- SkillProgressionService 首次学习明确写入 rank 0；本次首册改为 rank 1，原 rank 0 存档仍兼容保留。

## 主控阶段验证

- `skill_progression_service_test`、`skill_progression_save_integration_test`、`random_teleport_map_extent_test`：3 项 PASS，见 `outputs/test_logs/runner_results_adhoc_20260906_133138.json`；同轮技能书旧断言将背包槽数误当书本数失败，保留记录。
- 修正无书拒绝断言为比较操作前后完整背包，`skill_book_rank_upgrade_integration_test` PASS，`runner_results_adhoc_20260906_133217.json`。覆盖三本满级、等级/职业/无书/满级拒绝、保存失败回滚与成功事件次数。
- 随机传送在正式比奇地图固定 RNG 128 次验证四象限、越过旧 16.25 GU 范围、落点合法及非法地图拒绝。主源 ObjBase.pas SHA-256 `65D59610B8A1F7F4DCF76058A753651D1A97997AD273FC4DF8E468E65A989262`，`MapRandomMove` 约 9810 行。
- 新 APK 版本拟为 `71 / 1.20.0-gameplay-audio`。启动加载器用 manifest.baseCommit 对照 APK generated/build_info.json.git_head，防止保留的旧补丁覆盖新 APK；不删除用户补丁或存档。`device_lab_patch_bootstrap_test` PASS，`runner_results_adhoc_20260906_133527.json`。
- 上述属于施工期工作区结果，最终源码与 APK 身份待交付时补齐。
- 经验补充：`progression_test` 与 `player_level_up_effect_runtime_test` 在 `runner_results_adhoc_20260906_135740.json` PASS；死亡专项旧 fixture 的 1 级扣除已按新门槛变为 0，改用 7 级覆盖实际非零扣除后，`death_revival_home_failure_test` 在 `runner_results_adhoc_20260906_135848.json` PASS（0 engine errors）。保留上一轮失败，不将其认作通过。
- `runner_results_adhoc_20260906_135508.json` 出现并发 runner 同秒覆盖，不作为验收证据；已重新明确唯一 Godot 使用者，受影响专项独占重跑。
- `runner_results_adhoc_20260906_142156.json` 中图标、怪物近战几何、特殊攻击、目标法术、单位来源合同 5 项 PASS；其余 3 项失败分别返工，不把该轮记为全通过。
- `runner_results_adhoc_20260906_142516.json`：批量拾取事务及召唤物防御专项 PASS；刺杀新专项动态返回类型需显式声明后复跑。
- `runner_results_adhoc_20260906_142700.json`：`loot_runtime_item_policy_test` PASS，包含尸王正式 8 槽来源/增幅、固定 seed 512 次掷骰及掉落身份链。
- `runner_results_adhoc_20260906_142754.json`：`loot_stable_identity_save_test` PASS，覆盖疾风药水旧堆叠与 ID 堆叠兼容、真实文件保存/重载、错误 ID 拒绝、保存失败回滚、920xxx 正式技能书 ID 桥。Godot JSON 数值恢复为 float 属于解析兼容，测试按字段语义核对，不误认类型差异为身份丢失。
- 音频缓存复审修复：Enemy/Summon 的一次性静态 miss 会导致服务晚出现或退出再进入后永久静音。现有效实例共享复用、miss 使用类级 1 秒负缓存、旧实例释放后允许重发现，不影响玩法 RNG。`151535_215_25384` 的怪物钩子 PASS；`151455_199_2952` 的召唤钩子 PASS，该轮怪物测试自身释放夹具错误保留，未算全绿。
- 新增音频/掉落/技能/召唤场景均纳入正式 critical 去重清单。原地图、美术资源及批准的升级动画未修改；冻结 BGM 哈希复核一致。
- 剩余技能集成专项：`152839_269_29976` 五项全部 PASS、自然退出、0 engine errors；最终仅调整注释/函数名称的全职业普攻入口测试在 `152914_269_6532` 再次 PASS。烈火无目标不选择 Fire、不消费充能/冷却，保留既有普通攻击 fallback，不扩大成禁止普通空挥。
- 初始草案 `assets/audio/skills` 下 6 个未被正式映射引用的 WAV 已移至 `outputs/rejected_audio_draft_20260906` 可恢复归档，不随包发布；正式哈希去重声源位于 `assets/audio/sfx`，未删除主源或用户素材。

## 尸王掉落核查（不改人工概率）

- `runner_results_adhoc_20260906_150545_874_26248.json` 的掉落政策扫描通过全部运行怪物去重后的 **233 个物品身份/名称组合**，无 unresolved 身份。该轮音频服务、投射物音频生命周期、主城音乐亦 PASS；物品音频回滚夹具失败已按完整装备实例修复，在后续 `150920_304_22584` 中通过。

- 独占复测 `runner_results_adhoc_20260906_145136_613_11448.json`：掉落政策与完整身份存档/实际地面拾取 2 项 PASS，0 engine errors。实际拾取测试使用 `manager_evaluate_collection` 的一次性入口，避免生成时自动拾取后又手动重复调用信号监听器。错误正式 ID 不得回退成同名物品；排序两种顺序、一般合堆、技能书 920043 的真实保存/重载均覆盖。
- `runner_results_adhoc_20260906_144746_537_25420.json` 中怪物音频钩子与战士 canonical runtime PASS；当轮其余两项失败不计通过，刺杀实际防御专项继续返工。
- runner 增加按工作树路径命名的系统互斥锁，并将证据文件名细化到毫秒/PID，防同树并发 Godot 覆盖日志和误清理彼此进程。新入口已完成上述独占执行；不将旧并发结果用于验收。
- 预热保持轻量的正式输出名称映射，不为每个潜在槽构造完整物品记录；真实中奖路径才生成稳定身份记录，未改变掷骰次数、顺序和概率。

- monster_id 89 正式启用 8 个 item slot；7 个技能书槽 `BYPASS_UNCLASSIFIED / HUMAN_FROZEN_FAIL_SAFE_BYPASS` 保留 base，装备 230 为 `AUTO_BOOST_EXACT_X25`，`1/1000 -> 1/40`，上限未触发。
- 当前单只零掉落概率 `0.8374190763043114`，10 只全零 `0.16960103507654997`。因此该现象不能单独证明倍率失效；保留原始表、冻结分类和用户倍率，不擅自改成保底掉落。
- 修复身份输出链并保留原 `items` 兼容列表；`item_records` 将正式输出 ID 传入 LootPickup、批量入账和存档。920xxx 技能书通过 DPV2 明确 ID 表连接仅有 serviceIndex 的旧显示记录，非名称猜测。女性装备原有显式输出映射保持不变，源 ID 单独保留供审计。
