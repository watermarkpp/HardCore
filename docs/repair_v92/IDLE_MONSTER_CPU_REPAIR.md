# 闲置怪物查询与唤醒频率

2026-09-22 21:16，基线 b961cedf，尚未提交。用户明确要求闲置怪物只检查索敌范围内敌人、降低不必要的扫描频率，并要求排查手机 CPU 卡顿。此阶段生产 enemy.gd SHA-256 为 `c30bb568aaea85eff0613fc41674747708102ca5c07aad7c2196688ca53505d4`。

## 已确认的浪费与修复

真正无目标、无仇恨的怪物此前在拒绝索敌范围外的候选之前，仍会检查安全区和仇恨。生产 `_retarget_internal` 现在先使用该怪物已有的精确 ViewRange 矩形拒绝外部候选，再进行存活、安全区和静态视线判断。空仇恨表不再生成 keys/values 数组。已有目标、受伤仇恨、宠物拦截、追击边界和目标稳定策略保持原路径。

100次范围外候选的真实调用反例：安全区查询100→0，仇恨查询100→0，LOS始终0。原失败、fixture错误与修复、12项相关回归，以及固定12参战+50闲置的前后六次采样均归档在 `evidence/idle_acquisition/comparison.json`。该阶段后台维护总耗时约下降24%～25%，但它只是整个采样窗口内约10毫秒的小项，不是整帧下降25%。

后续按用户明确要求，真正停在出生点、无目标、无仇恨、无受伤待处理、无移动步的普通怪，现有后台 Timer 间隔由0.25秒改为0.5秒。初次仍按稳定serial错峰，并清除会跳过首次扫描的重复 retarget 延迟。回出生点保留0.25秒；共享玩家/召唤物发现缓存仍为0.25秒；Boss不进入该深度休眠路径。受击和仇恨事件继续立即退出休眠。

旧0.25秒唤醒中，retarget已有0.28～0.45秒截止时间，因此有些唤醒本来不会重新索敌。这里减少的是后台维护唤醒，不能表述成所有怪物的实际索敌次数一律从4次/秒减到2次/秒。自然恢复、状态和攻击截止时钟仍按实际经过时间推进；elapsed上限随调度间隔调整，避免正常0.5秒加调度误差被截断。

## 功能与工作量验证

- 新计时器反例先RED（实际仍为0.25秒），再GREEN。实际等待1.1秒只收到2次唤醒；玩家刚错过一次扫描后进入边界，下一次扫描约531毫秒获取目标；直接伤害同步唤醒，不等0.5秒。
- 第一组5/5 PASS：计时器、范围预算、密度运行、群怪、自然恢复。第二组6/6 PASS：目标获取、空间候选缓存、仇恨动画、安全区、HC运行、特殊怪运行。全部0引擎错误，完整进程退出正常。
- 固定62怪，12参战、50闲置，240物理帧，近/远各两轮。初末状态、真实伤害16次、总伤害、参战布局、physics调用数、fixture以及其他生产文件SHA一致。近/远交错执行，旧阶段样本另时段采集。

| 条件 | 唤醒调用，修改前→后 | 后台耗时中位，微秒 | process间隔p95中位，毫秒 |
|---|---:|---:|---:|
| 50闲置怪在附近 | 750→400 | 8145→5790.5 | 10.627→10.4665 |
| 50闲置怪在远处 | 750→400 | 8017.5→5812.5 | 10.6095→10.2735 |

维护调用减少46.7%，但每4秒样本只再节约约2.2～2.4毫秒后台CPU；不能声称游戏整体快46.7%。小幅墙钟差异有调度与采样时段影响，不能用来保证手机帧率。正式黑暗地带62怪、8场72格火墙又执行两轮PASS，实际使用OPTIMIZED，方向反例为0。

第二阶段完整原始样本、runner、RED/GREEN和哈希见 `evidence/idle_cadence/comparison.json`，归集入口 `tools/performance_v92/compare_idle_cadence.py`。suite registration PASS。

## CPU 与 GPU 的结论边界

手机卡顿可能来自CPU，当前证据没有排除它，也没有证明只由GPU引起。主线程负责AI、碰撞、技能和场景更新；整体CPU占用不高也不能排除主线程耗时超过帧预算。平均时间不足以排除偶发长帧。

此前隔墙保留追击目标的合成对照已经证明，实际造成攻击伤害的数量相同，完整AI数量仍可能从12增加到62，CPU成本明显上升。但是三张正式地图静止样本没有同样的寻路排队，不能宣布这是用户手机的已确认唯一根因。正式移动过程还在补测。

本机全部使用headless dummy，没有实际Android GPU或呈现帧数据。**功能/调用削减：PASS；手机CPU/GPU归因与60帧验收：NOT_RUN；全部性能目标：FAIL，尚未达成。** 原先Godot原生退出故障仍保留未关闭，不因本轮成功抹去。其余累计修复、净化、APK和推送任务继续。

## 22:20 查询入口与同场景最终复核

最后敌人源码SHA为c6a10aad923ca8bb59897938dd5f599a47d71566b1dec2b4c6cca3a8d08f797d。HC目标入口重复判断近战策略，100次查询原为200次策略判断，反例RED后改为100次；缓存scope缺键默认值也由临时字典/数组改为标量。现场空/非空/无效安全区和同帧变更保留原规则。相关6项PASS，正式黑暗地带62怪+8场火墙移动复测PASS；不是改变攻击范围或碰撞。

原21:40闲置阶段M30贴身p95+7.34%的FAIL保留于m30_idle_final_ab。新查询阶段8次运行全部功能PASS；严格可比的开阔追击p95中位14.806→14.792ms、贴身18.8125→18.3485ms。另两项比较BLOCKED：密集一轮攻击29→26；火墙一轮320→321实际物理tick。比较器写全量证据后仍以断言失败退出，没有剔除慢样本或放松相同工作量门禁。原始旧代码火墙26.344ms离群点也保留。

追加只观察时序的四次诊断，在未改生产的基线及当前源码都通过100ms预热卡顿复现第13怪停在距离2.0027337的位置、攻击3→0；无卡顿时停在1.986693并攻击3次。45次预热等待实际推进58～65物理tick；生产绕行使用真实墙钟。说明此原基准会随调度改变行为，不能把减少攻击当作优化收益。该诊断只解释可比性，不是性能数据，更不把2格外不攻击改成放宽范围。详见evidence/delivery_close/manifest.json。

用户随后明确要求先交付当前优化包、实机继续检验。因此剩余手机瓶颈归因和整体性能目标留给设备数据，不继续重复电脑基准直到出现好看数字。
