# Codex崩溃自检记录

## 2026-07-02 20:01 客户端重启

- Codex 全部进程启动时间集中在 20:01:56—20:01:58，说明桌面客户端在该时间发生整体重启。
- Windows Application/System 事件中没有 Codex 或 Godot 崩溃记录，Windows WER 也没有 Codex 崩溃报告。
- 重启后 Codex 主进程工作集约 1.6GB，当前线程渲染进程约 451MB；系统物理内存 15.89GB，检查时仍空闲 8.35GB。
- 当时没有 Godot 残留进程；上一装备套件正常完成，因此没有证据表明游戏项目、Godot 测试或荣耀90导致崩溃。

结论：最可能是超长线程叠加大段终端输出造成 Codex/Electron 渲染历史与主进程内存膨胀，随后桌面客户端自恢复重启；不是整机内存耗尽。由于没有 Crashpad/WER 转储，无法精确到某个函数。

后续约束：

1. 所有源码检索限定目录、文件类型和最大结果数，不向界面输出整份大文件。
2. 新测试先单项执行，最终只执行任务相关轻量套件；不重复运行全量测试。
3. Godot 测试继续采用独立进程、8秒超时、断言早停和仅清理本轮进程。
4. 图片批处理、数据生成和审计尽量在本地脚本完成，只回传摘要。
5. 非里程碑阶段不导出 APK，不启动长时间编辑器导入。
