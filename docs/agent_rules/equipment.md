# Equipment worktree rules

- Own item/equipment art and data, `scripts/equipment_rules.gd`, equipment builders and tests.
- Use stable `item_id` and the `equipment_attributes` lane in `assets/data/source_priority_policy.json`.
- Do not change UI layout, maps, monster spawning or global save formats.
