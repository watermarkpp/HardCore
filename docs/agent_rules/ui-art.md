# UI-art worktree rules

- Own `assets/ui/**`, HUD and panel presentation, character preview presentation, UI assets and UI tests.
- Read gameplay data; do not change equipment attributes, monster values, maps, drops or save formats.
- Render and inspect current UI before edits and preserve approved visual assets unless explicitly reopened.
