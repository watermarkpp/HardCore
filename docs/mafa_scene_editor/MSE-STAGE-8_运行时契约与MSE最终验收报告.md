# MSE Stage 8 运行时契约与 MSE 最终验收报告

任务：`MSE-STAGE-8`  
结论：通过，MSE Stage 0–8 全部完成。

## 最终交付

- 新增独立运行时快照读取服务：读取已构建的 runtime JSON，不读取编辑器工作区。
- 运行时入口校验 schema、必填字段、设计尺寸、门点目标、编辑器路径泄漏和 `build_sha256` 完整性。
- 提供基础消费查询：`is_blocked(tile)` 用于碰撞/移动，`entries_at(tile, kind)` 用于 NPC、门点、刷怪和区域事件接入。
- 任何篡改快照内容、引用编辑器工作区或缺失门点目标的快照均拒绝加载。

## MSE 完成状态

| 阶段 | 结果 |
|---|---|
| Stage 0–2 | Schema、空白 Chunk、地面笔刷与 Bake 完成 |
| Stage 3–5 | 素材校准、实例、地貌、手工碰撞与 Walkable 完成 |
| Stage 6 | NPC、门点、刷怪、安全区、光效、区域触发完成 |
| Stage 7 | 语义校验、批准、Build Runtime 与导出门禁完成 |
| Stage 8 | 运行时快照消费契约与篡改审计完成 |

## 最终验收

MSE 专项关键回归 10/10 通过，涵盖 Stage 0 至 Stage 8。素材目录校验：161 项可放置素材，`errors=0`，`warnings=0`。

## 交接边界

MSE 已完成，但并不自动把任意编辑器草稿写入安卓游戏。后续比奇区域施工应在编辑器中完成地图、批准并生成 runtime 快照后，再由游戏运行时集成任务有选择地消费；这样可以保持当前 APK 稳定并避免未经审核内容进入试玩版本。
