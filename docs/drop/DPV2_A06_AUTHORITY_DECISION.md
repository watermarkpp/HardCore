# DPV2 A0.6 Authority Decision

Date: 2026-08-27

Status: `USER_AUTHORIZED / A0.6 ONLY / PHASE_1_FORBIDDEN`

## Item identity

1. `service_index` is permanently classified as a legacy/source locator plus provenance. It is not a canonical, runtime, persistence, or long-term identity key.
2. All 53 A0.5 items with non-positive IDs must receive explicit positive canonical item IDs.
3. A unique existing formal project item ID takes precedence when one exists. If no historical formal number exists but the item identity is unambiguous, HardCore may allocate a new ID from a scanned, unused, project-reserved interval.
4. New mappings are permanent and explicit. Runtime derivation from item name, array order, or `service_index` is forbidden.

## Tier closure boundary

The 225 deterministic A0.5 Tier results are accepted for the A0.6 candidate basis. The eight unresolved materials/key items must remain unresolved until a subsequent human Authority decision. They may not default to COMMON or be assigned through name semantics alone.

## Monster role closure boundary

The 124 deterministic A0.5 role results are accepted for the A0.6 candidate basis. The 32 conflicts require individual canonical-level decision packages. V1 217-monster roles are seeds, not current identity Authority; `max/highest-role-wins` is forbidden.

## Frozen role decisions

- `VETERAN` is removed from the formal DPV2 Monster Drop Role taxonomy.
- The approved six `NEW_CLOTHES_BOSS` identities and their strict 1:1 clothing mappings are frozen.
- Canonical monster `225 暗之牛魔王` is frozen as `ENDGAME_BOSS @ 16` with `new_clothes_eligible = false`.
- `UNIQUE_GEAR_BOSS` is not a DPV2 Production role.

## Construction boundary

A0.6 may modify Authority/audit data, focused audit tools, reports, and tests. It must not modify Production Runtime, the 7032 drop slots, actual probability logic, canonical monster identity, map monster data, map data, or the map editor. A0.6 must pause after producing the 53-item closure and the eight-item/32-monster human decision packages; it must not enter Phase 1.
