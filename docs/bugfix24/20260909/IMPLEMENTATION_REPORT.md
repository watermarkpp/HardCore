# 2026-09-09 两包整合施工记录

状态：实现与分项验收进行中；本阶段 APK 尚未构建。当前集成锚点 `3a9ea50e`，最终状态同时见 [24项台账](ACCEPTANCE_MATRIX.md)。

## 授权与保护范围

用户要求完成 V3/R3 收尾报告和24项施工包，整合后准备下一阶段 APK。初始主树 `cf1d2718befdef7e6cc2fb274fd91ce0759d105f`，bootstrap通过；原始用户V3工作树的暂存内容保持原样。附件是需求和待核验技术材料，旧暂停提交建议不替代本次明确施工授权。

用户已明确批准属性面板并要求字号减小2–3号，当前采用标题20/正文14。面板位于最上层、按内容收缩；优先背包空区，必要时最多遮挡物品约一半并保留选择入口；已装备物品详情放装备栏内部，允许覆盖纸娃娃。批准已经生效，不再等待重复校准。

爆率概率/槽位/抽样、正式装备属性、人工地图/素材保持冻结。本次允许完整掉落实例、合法落点及显示；怪物动态硬碰撞按用户明确要求恢复，同时保留数量、速度、节拍、正式投影和空间索引。Snapshot V2与现有技能伤害owner保持唯一。

## 已集成与分项证据

| 范围 | 当前实现和验证 |
|---|---|
| Loading、死亡、升级 | `2ceb73b0`：首次await前隔离伤害、死亡优先、成功升级事务后恢复HP/MP；失败不恢复。V3延迟攻击读取typed combat_epoch，六条既有投递通道主树通过。 |
| WORLD与锁定 | `7468943c`：法术锁定10 GU，正式起手/释放/伤害重新验证WORLD；MC/SC读取既有幸运公式。`world_cast_stage_b`保留候选运行证据，不能当干净HEAD全包结论。 |
| 银行与金币 | `41471a79`：10万转账、玩家/共享两级上限、旧溢出归档、WAL/CAS和序号防重、保存失败回滚。`6859e110`接入仓库按钮，背包/仓库2/2通过，见`evidence/bank_ui_stage_f`。`5dd79d94`新增正式拾取、单/批出售、任务合并奖励的金币上限与保存失败重试专项，通过并归档`evidence/t30_gold_cap`。 |
| 地面掉落 | `705000eb`：WORLD合法落点、确定性有限候选与失败队列重试；拾取距离及最新墙体检查，保存失败保留源物。`bank_and_loot_stage_c`及`loot_world_first_failure`保存通过/失败证据。 |
| 完整实例与攻杀 | `692cbba5/f3bb6cc1`等：完整实例贯穿背包、装备、共享仓库及保存回滚；非法nested拒绝，旧普通记录兼容。攻杀四种模式一次proc、多目标共用结果，见`instance_stage_d`、`proc_luck_stage_e`。小极品默认5%产生一项+1属于工程默认，不称原版概率事实。 |
| 属性面板 | `dd919c21/74c7e38a`：批准的字号、内容自适应、背包空区/装备栏定位、结构化record、负重、最后实际选中实例及取消回退/清空；禁止多选穿戴。相关主树专项通过。 |
| 音效 | `584b8daa/fcc466b3`：SFX增益50%、预算与早拒绝、首次战斗提示锁存；BGM独立。20/50全活跃真实physics八条件严格对照通过，见下节。 |
| 可视内容 | W6限定雷电WORLD足点排序、正式精英/Boss标记、exact ID怪物显示名、完整实例字色与精确tier装备光柱；主树四项专项通过，见`evidence/w6_main_integration`。ID910008主源精确图像缺失，采用有来源缺失证据的项目SVG补图，不假称原素材。 |
| V3功能 | `81f3cb89/06d10038`：ID50投递及V3路径/硬占位/观察记忆/冻结目标/代次接入；主树七项功能通过，见`evidence/v3_main_integration/06d10038_162631`。`dd31a610`非战斗门禁与四个既有projectile ID再验2/2通过。`dcf98d24`普通Boss ID162连续攻击80项断言通过，保留首次30秒超时与60秒成功日志。 |

银行接受的是单进程同步共享仓库事务；未声称具备多进程OS锁。存档专项覆盖跨角色与失败恢复，但未把故障注入等同真实系统强杀，也未声称全部异步组合已验。

## 性能与渲染证据边界

W4严格对照采用旧版`cf1d2718`和候选`ef827331`，同一地图、同字节headless预取兼容层；20/50 × on/off，每条件60physics预热+240physics采样，所有actor至少实际攻击一次。两侧runner自然PASS；开启音效整帧墙钟p95为50只旧12.794/新12.348ms、20只旧9.374/新9.305ms。原始数据、hash及主树功能冒烟已归档`evidence/audio_full_frame_strict`。攻击计时器相位导致请求量不同，所以不把这组数据称为等请求量加速、音频线程CPU或Android混音结论。早期只有3/11活跃怪的探针仍保留，不能替代此证据。

V3 REV07目前仍不达标：此前严格WORLD墙场景10/20/30只的p95旧7.432/8.206/8.538ms，新8.995/10.275/11.112ms。四场景（开阔追击、持续近身攻击、真实障碍、密集群体）矩阵正在补齐，不能用功能PASS替代性能门槛，也不能降低怪数/碰撞/玩法频率制造提升。

`5fbef39d`仅修复两处已观察到的headless预取入口，保留可视/Android异步路径。在主树9a5f7158上，正式dummy渲染三场景连续三轮均3/3 PASS，完整逐轮raw见evidence/v3_main_integration/final_9a5f7158。原始stderr保留ObjectDB/resource清理等现有allowlist文本；runner的engine_log_errors=0不等于原始日志零ERROR。本轮未放宽allowlist。

## 当前未完成项

- W1：153个runtime ID、32类投递审计已有；初始6个物理远程ID、224目标魔法/邻接物理、9个非战斗宝箱已在主树通过，见`evidence/v3_main_integration/w1_initial_d114eb53_165323`。ID62原profile字段丢失已在集成前修正，候选身份未提升为primary。spit/gas/line/mixed/guard 17个精确ID新通道已集成，675005cd真实Actor专项通过，799c5328 Critical中同样通过。
- 12个缺少exact actor来源的ID继续DATA_HOLD，不按名称或变体猜配。226–234另有人工固定非战斗权威，可接逐ID禁止自主攻击门禁并保留受伤/掉落，不能据此补造actor class。
- REV07四场景公平对照、最终Critical和固定渲染复验、冻结范围最终复核、APK构建与身份/签名/资源闭包验证仍未完成。

## 构建与交付门

导出配置为 `72 / 1.21.0-r3-closure`，可见名HardCore，兼容包ID `com.personal.mafaoffline`。`73728f20`预检通过，但不是成品。显式对比现存9月6日版本71基包，避免脚本旧默认v38路径不存在。初次project版本未同步的问题已修正。

冻结阶段检查`frozen_scope_checkpoint.json`为7031项中7030项原hash一致；唯一旧测试差异是无地图死亡队列夹具的位置provider适配，生产WORLD由真实场景验证。后续获准行为catalog生成有来源hash级联，最终必须做语义复核，不能把此阶段hash结论复用成最终零差异。

完成剩余实现后固定源码提交，运行必要最终回归，再隔离导出并核对APK版本、签名、build-info、资源和SHA256。当前没有新APK或设备验收结论。历史失败原始资料保留，已被同名runner覆盖的早期raw明确区分，仅存JSON时不冒称完整逐轮日志。

## 2026-09-09 19:22 集成验收增量

- 主树 `799c5328` 完整 Critical 为 318/344 PASS，26个失败完整原始日志与SHA256已归档 `evidence/v3_main_integration/final_799c5328/critical`。
- 失败项完成正式WORLD/READY与独立仓库夹具适配；ID18起步位置对齐已授权喷吐射程；queue-only落点适配保留生产WORLD专项，未改爆率抽样。Loading等待预算对齐既有过渡夹具5秒，保留覆盖、单飞、隔离和READY断言。
- `9a5f7158` 24个失败项加10个受影响路径/索引专项共34/34 PASS；三轮smoke/warrior_visual/player_movement_respawn各3/3 PASS。`0247b8d0` 最后音频和Loading两项2/2 PASS。这是全套基线加最终增量复验，不是单次最终HEAD 344/344。
- 投影缓存与仅用于布尔占位的无序候选查询已整合；缓存验证同帧移动、地图/修订/提供者失效，旧有排序与投射物查询语义保留。最终严格性能对照待执行，不用减少投影调用的诊断替代帧时间验收。
- 用户补充旧APK在其他手机不停攻击：未证实设备根因，已排除fresh默认自动攻击；正在补充输入事件防护与专项。未连接设备，不声称已复现或解决三台手机现象。
- 覆盖安装预检通过：显式对比桌面版本71基包，配置版本72；新增同包名、同签名证书摘要、versionCode递增检查。过期启用热更新包的定向清理和人物/共享仓库字节保留专项通过；实际新APK核验尚待构建。
