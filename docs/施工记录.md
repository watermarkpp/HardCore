# 施工记录

本文件是按时间追加的工程施工账。每项记录必须包含目标、实际操作、依据、产物、验证、状态和未完成项；不得用计划冒充完成。长期规则写入项目总纲，结构变化同步刷新`项目总目录.md`，资源来源和扫描结果同步写入专项资源目录。

## 2026-07-15：NPC-FACING-1 经典 NPC 六视图激活与交互转向

- 目标：NPC 出生默认朝实际地图中心，玩家交互时转向玩家；保持原服务入口不变。
- 依据：主客户端 `Npc.wil/WIX` 与主规则 `MirClient/Actor.pas`。原客户端运行时只取 `dir mod 3`，但外观 0—22 的资源中实际存在六组真实前后视角。
- 实际操作：建立可重复 WIL 图集构建器；启用六组原始视角并映射工程八方向；接入 `NPCActor/NPCVisual/game_root/map_editor_runtime_bridge`；显式服务端 `appearance/Image` 优先，缺失时使用稳定服务类型兜底。
- 产物：23 套 NPC 待机图集、逐帧来源清单、八方向接触表、Godot 实际加载截图和专项自动测试。
- 验证：专项 1/1、比奇 24/24、完整关键回归 51/51 通过；非 headless OpenGL 截图通过。
- 状态：本阶段完成。主服务端 Crystal `Server.MirDB` 全量 NPC 表解析与精确 `Image` 对应留给 `NPC-DATA-2`，不得把当前兜底外观冒充服务端原值。
- 完整报告：`docs/NPC-FACING-1_经典NPC交互转向施工报告.md`。

## 2026-07-15：项目瘦身与工具归档

- 目标：清除无用资料、保留客户端/服务端证据和地图编辑器能力，补齐可重复维护工具。
- 实际操作：审计当前工程及 `Documents/My Games`；确认后者仅为其他游戏目录。删除 6 组扫描 smoke 输出和所有 Python `__pycache__`；未删除 `dev_art_sources`、地图编辑器工作区/备份、正式验收产物或 Godot 工具。
- 产物：新增 `tools/project_maintenance.py` 和 `docs/PROJECT-OPTIMIZATION-1_项目瘦身与工具归档报告.md`；审计结果为 `outputs/validation/project_maintenance_audit.json`。
- 验证：维护工具语法检查通过；正式多端验收档案仍保留；项目输出约 2.92GB，原始端资料约 14.60GB。
- 状态：完成。后续新增资料必须按端归档，临时产物不得进入正式验收目录。

## 2026-07-15：旧独立工程复核与重复产物清理

- 依据：用户补充旧工程位于 `C:/Users/Administrator/Documents/Codex`。
- 实际操作：核对 `2026-06-28/xian/work/legend176_game` 与当前工程的路径、大小和 SHA-256；确认旧工程的正式输出、地图编辑器工作区、端资料和工具均已迁入或在当前工程有对应版本。
- 清理：删除旧工程 `.godot`、`.tmp_asset_inspect` 和与当前工程逐文件哈希一致的旧 `outputs`；保留旧源码、资料、地图编辑器工作区和文档作为回滚证据。
- 状态：完成。未发现需要从旧工程补迁的游戏运行文件；后续以当前工程为唯一施工目录。

## 2026-07-14：完整客户端知识库与黑铁头盔追溯

状态：实现与自动验证完成；未构建APK，等待用户下一次打开工程时进行视觉确认。

目标：

- 不依赖数据库分类或文件名，完整扫描一个客户端的全部资源容器和索引帧。
- 以经典客户端`StateItem.wil #344`为黑铁头盔装备图标的确切造型依据，在全量知识库内反查世界外观层。
- 若完整客户端确认没有对应世界层，则基于已核实图标生成项目扩展八方向动作层；不得继续使用已证伪的其他头盔或发型。

已执行：

- 当前唯一工作目录确认为`C:/Users/Administrator/Documents/我的刷子游戏`；旧工程内容先迁入当前工程后使用。
- 完整迁入经典客户端`dev_art_sources/reference/mir2_client_raw`，共1167个文件，包含`Data/Map/Wav`；经典`StateItem.wil #344`与当前装备栏黑铁头图标一致。
- 完整解包2013客户端至`dev_art_sources/external/mir2opensource_full`；`Data`含122组WIL/WIX、962251个索引帧。
- 根据配套`PlayerObject.cs`和`Frames.cs`纠正后期客户端人物步长：普通人物2224、Killer 1456，其他家族用关联库帧数结构推导；否定了把所有资源统一按600帧切割的旧做法。
- 建立跨分类动态外观目录：34个来源、530套外观、394个非空头部区域；`Hair/Hum/HumUp/Weapon/Helmet`和外部`hum.rar/2hairs.rar`均按实际结构进入同一目录。
- 已证伪：2013 `Hair.wil LOOK2`只是狭窄发型条，不是黑铁头盔；2013 `Helmet.wil`六组均为角盔、尖盔或开放式造型；`hum.rar`的102套是整衣/翅膀，头部基本裸露；`2hairs.rar`四套为普通发型。
- 新增`tools/scan_complete_client_headwear.py`：把完整客户端每个索引帧写入SQLite，记录实际尺寸、偏移、非透明像素数、像素SHA-256和头部覆盖标记；头部候选再做真实像素解码和去重排序。
- 全量扫描完成：122/122个资源库、962251/962251个索引全部入库；962250帧有效，唯一无效项为`Title.wil #619`，仍以无效记录保留而没有跳过。
- 头部几何查询共解码332460帧，得到319857组不重复头部像素；SQLite `PRAGMA integrity_check=ok`，数据库SHA-256为`4c96ed1aae5ecb1e2660a2a58ae0007e17a87a2ce049d4b16d1925d327ea380a`。
- 最终证据结论：配套客户端运行源码`DrawHead`只消费`HairLibrary`，其正确步长的全部发型以及独立`Helmet.wil`六组均不含与`StateItem #344`一致的黑铁头世界层；完整帧目录用于证明没有因错误文件名漏扫其他容器。
- 按用户授权改为确定性生成：`tools/build_world_helmet_asset.py`直接使用`StateItem #344`透明像素作为正面，生成侧面/背面八方向；2013 `Hair LOOK2`只提供五动作逐帧头部位置，不复制其任何像素。来源清单明确标记为`project-generated presentation asset`，不冒充客户端原帧。
- 人物血条从角色逻辑原点`x=0`修正到经典战士可见身体中心`x=-8`，纵向保持`y=-95`；血条仍是玩家节点的独立子节点，不再受攻击或受击动画帧影响。

产物：

- `outputs/resource_catalog/mir2_client_data/catalog.json`：122个资源库结构总目录。
- `outputs/resource_catalog/mir2_cross_category_appearances/manifest.json`：正确动画步长的跨分类外观目录。
- `outputs/resource_catalog/complete_client_frame_catalog/frame_catalog.sqlite`：完整客户端962251行逐帧知识库。
- `outputs/resource_catalog/complete_client_frame_catalog/manifest.json`：全量统计、库级哈希和前512个不重复候选。
- `outputs/resource_catalog/complete_client_frame_catalog/top_unique_head_candidates.png`：不按名称分类的全库头部几何候选图表。
- `outputs/visual_acceptance/black_iron_helmet_directions.png`：由正确黑铁头图标生成的八方向基准图。
- `outputs/visual_acceptance/player_states/idle_south.png`、`attack_east.png`、`hit_south.png`：Godot真实渲染窗口截图。
- `assets/art/characters/warrior/wear/helmet/black_iron_helmet.source.json`：图标、完整扫描、动作锚点、生成方式和五套图集哈希的来源记录。

验证：

- Python语法检查通过：完整扫描器、跨分类目录器、头盔生成器和WZL解码器均可编译。
- Godot专项4/4通过：`complete_client_resource_catalog_test`、`warrior_visual_test`、`developer_character_test`、`player_struck_lock_test`。
- 非headless真实渲染捕获通过：`PLAYER_HELMET_HEALTH_CAPTURE_PASS`，已重建待机向南、攻击向东和受击向南截图。
- 关机前最终回归7/7通过：完整客户端目录、地图硬边界、地图编辑器运行桥、装备界面、装备客户端美术、战士视觉、受击硬直；`verify_judgement_staff_atlas.py`另行通过5动作、8方向、左上行和原始像素校验。
- 本任务按用户要求不构建APK；下一步先由用户查看截图。用户视觉验收仍待确认，不写成已验收。

## 2026-07-14：ChatGPT黑铁头素材输入包

状态：完成。

- 新增`tools/prepare_black_iron_chatgpt_inputs.py`，从当前工程的确切来源可重复生成ChatGPT所需6张输入图。
- 图片1使用经典`StateItem #344`透明像素裁切后按最近邻放大16倍，放入512×512透明画布；同时保留28×32原始源图作校验存档。
- 图片2至图片6直接复制当前运行时战士待机、行走、攻击、受击、死亡RGBA图集，没有二次缩放或改色。
- 输出目录：`outputs/chatgpt_inputs/black_iron_helmet/`；`manifest.json`记录上传顺序、尺寸、方向、帧数、来源路径和SHA-256。
- 校验通过：6张必传图均存在且为RGBA；尺寸依次为512×512、768×1280、1152×1280、1152×1280、576×1280、768×1280。

## 2026-07-14：黑铁头盔后向三视图验收与透明化

状态：方向造型通过，已生成透明参考素材；尚未作为运行时动作图集接入。

- 输入图：`C:/Users/Administrator/Downloads/ChatGPT Image 2026年7月14日 11_22_40.png`，尺寸2172×724；左、中、右依次确认代表正后方N、右后斜NE、左后斜NW。
- 三个视图均未露出正面眼缝，左右后斜视图结构互为对应，补齐了此前方向图缺少的背面与后斜面信息。
- 原图虽然使用品红背景，但不是严格单色：没有透明像素且背景存在轻微颜色渐变，因此没有采用精确颜色直接删除。
- 按边界自动采样、软蒙版和去品红溢色方式生成`outputs/chatgpt_inputs/black_iron_helmet/generated_references/rear_n_ne_nw_transparent_v1.png`。
- 透明结果校验：RGBA 2172×724；完全透明像素1,255,339，完全不透明像素310,019，半透明边缘像素7,170；四角全部透明；可见区域中检测到的品红色残留像素为0。
- 本图定位为黑铁头盔方向设计参考，不等同于192×160逻辑帧的正式五动作运行时图集，也不替代逐帧头部锚点校准。

## 2026-07-14：黑铁头盔八方向五动作正式接入

状态：代码、运行资源、来源记录、自动校验和Godot运行截图完成；等待用户依据截图做最终视觉验收，未构建APK。

- 将用户确认的透明后向三视图固化到`dev_art_sources/reference/generated/black_iron_helmet/rear_n_ne_nw_transparent.png`，将此前正面/侧面概念图固化为`front_side_direction_concept.png`，两者纳入来源哈希。
- 升级`tools/build_world_helmet_asset.py`：S使用确切`StateItem #344`透明像素；N、NE、NW使用已确认后向几何并以最近邻缩小、映射回`#344`金属色板；E、SE、SW、W继续由确切图标确定性生成并以正/侧概念图校对结构。
- 五动作图集全部重建：待机4帧×8方向、行走6帧×8方向、攻击6帧×8方向、受击3帧×8方向、死亡4帧×8方向，共184个192×160逻辑帧。客户端`Hair LOOK2`仅提供每一帧的头部位置和运动，不复制其像素。
- 来源清单升级为schemaVersion 5，如实记录生成式像素仅限N、NE、NW，正面仍为确切客户端图标；不再把整套素材错误标记为非AI生成。
- 新增`tools/verify_black_iron_helmet_asset.py`及`outputs/validation/black_iron_helmet_asset_report.json`：184帧全部非空，五套图集八方向互异，行走/攻击/受击/死亡每个方向均随帧移动，全部帧与重盔甲人物头部水平重合。
- 扩展`tests/player_helmet_health_capture.gd`，真实捕获八方向待机及待机/行走/攻击/受击/死亡动作样例；新增`tools/build_black_iron_helmet_acceptance_sheet.py`合成`outputs/visual_acceptance/black_iron_helmet_runtime_acceptance.png`。
- Godot专项6/6通过：`complete_client_resource_catalog_test`、`warrior_visual_test`、`developer_character_test`、`player_struck_lock_test`、`inventory_equipment_ui_test`、`equipment_client_art_test`；非headless OpenGL运行截图通过`PLAYER_HELMET_HEALTH_CAPTURE_PASS`。

## 2026-07-14：黑铁头运行加载链根因修复

状态：完成；此前黄色头盔验收图作废，已用新黑/灰八方向外观重新导入并截图。

- 用户根据截图指出运行外观仍是黄色旧头盔。底层核查发现源`black_iron_helmet_idle.png`已更新，但Godot实际加载的`.godot/imported/black_iron_helmet_idle.png-*.ctex`修改时间比源PNG早约11小时；先前结构测试只检查资源路径和图集尺寸，没有验证导入缓存实际像素，因此产生假通过。
- 重新提取唯一八方向母版：N/NE/NW使用确认后的后向三视图；E/SE/S/SW/W使用九格新外观的第2/3/4/6/7视觉格；旧`StateItem #344`派生世界像素用量明确为0。
- 强制执行Godot资源导入后，`.ctex`修改时间晚于新PNG，`CacheIsFresh=True`；随后重新运行非headless OpenGL截图。
- 新增`tools/verify_black_iron_runtime_import.py`，同时校验CTEX时间、运行截图像素模板和深灰中性色。最终南向新图集与截图像素匹配度0.951161，匹配位置`(258,225)`位于人物头部，有效像素平均RGB为`[72.4,70.5,67.1]`，确认不是旧黄色缓存。
- 相关Godot回归6/6通过。后续任何运行美术验收都必须在截图前刷新导入缓存，并验证截图像素而不能只验证资源路径。

## 2026-07-15：黑铁头盔尺寸与W朝向二次校准

状态：实现、真实渲染截图、自动回归和用户视觉验收全部完成；未构建APK。

- 依据`Helmet.wil`全部6套外观、192个待机样本的方向p75包络校准尺寸；首轮0.85仍被用户判定偏大，因此在现有尺寸上再次缩小15%，最终`runtimeEnvelopeScale=0.7225`。
- 待机首帧实际生成尺寸由上一版约14–18×16–20像素降至12–15×14–17像素；校验器逐帧限制184帧均不得超过对应方向真实客户端p75包络的72.25%。
- 第7格W最终按人物身体实际朝向水平翻转，清单明确记录`W.horizontalFlipApplied=true`；用户已确认朝向正确。
- E向原概念图比例过窄，按真实客户端E方向包络将待机/行走共用外观由12×15修正为14×15；用户已确认第3、10张比例正确。
- 南向死亡第3、4帧不再复用站立正面眼缝外观，也不把整张正面图旋转90度；改用黑铁头自身N/后顶部几何生成14×8、14×7俯视倒伏帽冠，仍跟随逐帧头部锚点。用户已确认第13张正确。
- 删除5套黑铁头图集对应的可再生CTEX/MD5缓存后强制重导入；确认CTEX时间晚于PNG。非headless OpenGL截图通过`PLAYER_HELMET_HEALTH_CAPTURE_PASS`。
- 实际加载像素校验通过：`cache_fresh=1`、南向模板匹配度`0.975443`、有效像素平均RGB=`[76.3,74.1,70.2]`，确认截图使用本轮深灰图集。
- 最终专项回归6/6通过：`complete_client_resource_catalog_test`、`warrior_visual_test`、`developer_character_test`、`player_struck_lock_test`、`inventory_equipment_ui_test`、`equipment_client_art_test`。
- 最新验收图：`outputs/visual_acceptance/black_iron_helmet_runtime_head_zoom.png`与`outputs/visual_acceptance/black_iron_helmet_runtime_acceptance.png`。
- 为排除Codex界面对同一路径图片的显示缓存，Godot最终直接写出13张新版本文件`player_states/v07225_w_flipped_20260715b_*.png`；验收合成图不复用旧路径。
- `tools/verify_black_iron_runtime_directions.py`确认八张最终方向截图全部晚于当前图集，逐方向像素匹配最低`0.926700`，W运行时翻转为1。报告见`outputs/validation/black_iron_runtime_directions_v07225_w_flipped.json`。
- 用户最终验收通过的图片：`outputs/visual_acceptance/black_iron_helmet_runtime_head_zoom_v07225_w_flipped_20260715b.png`与`outputs/visual_acceptance/black_iron_helmet_runtime_acceptance_v07225_w_flipped_20260715b.png`。

## 2026-07-15：比奇怪物经典客户端动作补全

状态：完成；构建器、运行资源、目录修复、像素校验、Godot 回归及视觉验收图均已完成；未构建 APK。

- 修正此前把“存在五张文件”误当成“五动作齐全”的审计口径；森林雪人、食人花、钉耙猫、稻草人、半兽人的生成式静态伪动画已全部替换为经典客户端 WIL 原始动作帧。
- 重写 `tools/build_bich_client_common_monsters.py`，直接使用当前工程内 `dev_art_sources/reference/mir2_client_raw/Data`、`tools/vendor/extract_wil.py`、`Actor.pas` 动作表及 `GetOffset` 公式，生成 11 种比奇常见怪物、55 张五动作八方向图集。
- `tools/build_bich_client_undead.py`去除旧工程路径依赖；`tools/build_monster_animation_catalog.py`改为精确怪物名优先，修复僵尸1至僵尸5等资源存在但目录误判缺失的问题。
- 动画目录由 formal 23 / provisional 9 / missing 182 更新为 formal 42 / provisional 0 / missing 172。
- 独立重解码源 WIL 并逐逻辑帧比较：2,384 帧，0 失败；四项 Godot 专项测试全部通过。
- 验收图为 `outputs/visual_acceptance/bich_monster_original_client_actions_20260715.png`，完整证据见 `docs/BICH-MONSTER-ANIMATION-1_比奇怪物客户端动作补全报告.md`。

## 2026-07-15：本地客户端与服务端多端全量拆解收口

状态：完成；全量扫描、58端独立分档、异常归因、数据库完整性、哈希与覆盖率验收均已通过。

- 对`dev_art_sources/`的38,887个文件、14,595,954,010字节完成逐文件哈希与内容优先扫描；未哈希文件为0，磁盘文件数和字节数与SQLite完全一致。
- 建立58个互不覆盖的端档案；完整客户端、服务端数据库、客户端/服务端源码、共享组件、资源压缩包和解包来源分别保留身份。每端均有独立`manifest.json`、`files.csv`、`semantic_hits.csv`、`resource_libraries.csv`和`maps.csv`。
- 全局库收录243个资源库、1,663,124个全路径逻辑帧、8,057张MAP画像和16个压缩包；跨端相同哈希只进入对照表，不合并归属。
- 22项解析异常全部完成归因：3个缺密码压缩包、18个0字节加密解包占位、1个与传奇MAP同扩展名的DelphiX示例文件；未发现未知异常。
- 新增`tools/verify_complete_local_mir_scan.py`与`outputs/validation/complete_local_mir_scan_acceptance.json`，12项验收全部通过；数据库SHA-256与清单一致，SQLite完整性为`ok`。
- 完整报告见`docs/COMPLETE-LOCAL-MIR-SCAN-1_本地多端全量拆解验收报告.md`。下一阶段回到NPC施工，但必须先按端提取再横向对照，不得把多个私服端合并为无来源的统一数据。

## 2026-07-15：空间规则统一、Godot 稳定性修复与最终清理

状态：完成；关键回归 49/49，编辑器/游戏连续启动退出 10/10，新增原生崩溃事件 0。

- 新增`WorldSpatialRules`，统一世界、玩家、怪物碰撞层，安全区、地图障碍和边界查询。
- 玩家和怪物共同使用`WorldBackground`；比奇编辑器障碍、手工占位及设计尺寸边界全部进入统一逻辑查询。
- 安全区固定为实际复活点圆心、9逻辑格半径；怪物AI与主场景兜底不再重复计算不同口径。
- 修复受控怪物漂移、控制锚点过期、控制期间旧延迟攻击继续命中、地图焦点移动不刷新源碰撞块、编辑器NPC库存身份丢失。
- 新增`world_spatial_contract_test`并纳入关键测试；完整49项全部通过。
- Windows事件日志表明已知Godot原生崩溃集中在首次全量导入；隔离`outputs`等非资源目录后，工作区健康检查和10次启动压力测试均通过。
- 删除最终测试日志和Python缓存；保留净化后的`.godot`缓存、地图编辑器备份、原始多端证据和正式验收产物。
- 完整报告：`docs/WORLD-SPATIAL-STABILITY-1_空间规则统一与Godot稳定性报告.md`。

## 2026-07-15：客户端与服务端主辅资料分级

状态：完成；四条资料线、唯一主资料、辅1/辅2/辅3权重、证据化降级工具和机器验收均已落地。

- 以已验收的58端完整目录为范围，分别建立客户端资源、客户端规则、服务端数据、服务端规则四条独立查询线，禁止把像素、数据库和源码规则混为一套优先级。
- 客户端资源主资料定为`client.classic_raw_complete`；服务端数据主资料定为目录干净的`server.crystal.cjlaaa`；客户端与服务端规则分别以两个`A-rule-source`经典源码入口为主。
- 内容最丰富但混有Git、BMP和程序文件的`server.angelk727_full`定为服务端数据辅1，只能补主资料已有证据证明的缺项。
- 新增`assets/data/source_priority_policy.json`和`tools/source_priority_guard.py`。辅1、辅2、辅3必须逐级证明所有更高层缺失、不可用或不兼容；没有证据或越级时工具直接拒绝。
- 镜像权重为0，仅用于哈希复核；隔离端不得进入运行层。跨端组合默认禁止，确需组合时必须逐项保留端标识、原路径、采用原因和授权结果。
- `tools/verify_source_priority_policy.py`验收通过，12项检查全部为真；报告见`docs/MIR-SOURCE-PRIORITY-1_客户端与服务端主辅资料分级报告.md`。

## 2026-07-15：正式游戏图标与品牌开场动画

状态：完成；母版归档、图标、Godot加载画面、动态开场、文字、启动跳转、OpenGL截图与完整关键回归均已通过；未构建APK。

- 将用户提供的1254×1254骷髅图按原始字节归档到`dev_art_sources/user_provided/branding/game_icon_master_20260715.png`，母版SHA-256为`079c9afa23139a73b79ca6cb5910c46678ac3910d191803d1e0c6f78c1904664`。
- 新增`tools/build_brand_assets.py`，只做确定性缩放与横屏黑底排版，生成1024×1024正式图标、192×192 Android图标和1280×720启动画面；没有生成式重画。
- 新增`scenes/brand_intro.tscn`与`scripts/brand_intro.gd`，实现主体淡入放大、红色余辉脉冲、文字显现和黑场转场；文字严格为“刷是一种状态，刷没有目的没有终点”。
- `project.godot`主入口改为品牌开场，Godot内置加载画面和应用图标统一使用正式品牌资源；Android导出预设显式使用同一图标。
- 动画1秒后允许键盘、鼠标或触屏跳过，不操作时自动进入原`character_select.tscn`。
- Godot导入和专项测试通过；真实OpenGL截图为`outputs/visual_acceptance/brand_intro_20260715.png`；完整关键回归50/50通过。
- 完整说明见`docs/BRAND-INTRO-1_游戏图标与开场动画报告.md`。

## 2026-07-15：战士技能八方向动画与烈火武器头吸附

状态：完成；全面扫描后的主资料复核、底层修复、OpenGL实机截图、战士专项与完整关键回归全部通过；未构建APK。

- 复核主客户端Magic.wil及主客户端Actor/magiceff规则，确认攻杀、刺杀、半月、烈火的八方向公式与源基址正确；基本剑术、野蛮继续使用正式攻击动作，不显示旧V形占位。
- 修复Magic.wil相对人物偏右32、偏下28的角色原点错误。
- 查明裁决攻击有9帧被旧`192×160`武器画布裁掉；武器改用独立`192×224`画布并与身体共享角色原点，全部3496个穿戴源帧越界为0。
- 烈火旧图曾被缩至52%，导致火焰缩回人物和裁决中部；现恢复1:1原始像素并拆成4张1920×1920分片，删除旧缩小图。
- 新增通用逐帧附着逻辑：从Weapon.wil计算当前武器头，从Magic.wil计算烈火起燃点，按已装备武器、方向、帧自动对齐。
- `tools/audit_warrior_skill_visuals.py`通过：穿戴3496帧、特效192帧、裁决新裁切0、烈火48帧附着元数据齐全。
- 战士专项14/14、完整关键回归51/51通过；报告见`docs/WARRIOR-SKILL-VISUAL-AUDIT-2_战士技能八方向动画审计报告.md`。

## 2026-07-15：COMPLETE-ITEM-SYSTEM-1 完整物品与地面掉落外观

状态：完成；未构建APK。

- 依据主服务端`server.crystal.cjlaaa/Server.MirDB`版本105完整解析1349条ItemInfo；538个非装备物品进入运行目录，808条私服/后期装备独立保存在参考层，既有175件已验收装备继续作为唯一可穿戴层。
- 背包、状态和地面外观严格按主客户端`Items/StateItem/DnItems`、辅1同索引、分类补图三级选择；主端有效地面原图521条、辅1补帧565条、分类补图263条。1×1假帧现在按不可用处理，不再冒充原图。
- 接入血瓶、蓝瓶、太阳水、雪霜、回城卷、随机传送卷、地牢逃脱卷、祝福油、修复油、战神油、金币、技能书、矿石、材料和任务物。普通药瓶使用原服务端分跳恢复公式；随机传送调用统一空间契约，不能穿越地图硬边界。
- 强制刷新Godot导入缓存并把测试升级为`ResourceLoader.exists`，保证不是“PNG已修改但CTEX仍旧”的假完成；已知运行物品不再显示彩色菱形占位。
- Equipment回归16/16、Bich回归24/24、工作区健康检查全部通过；真实OpenGL验收图为`outputs/visual_acceptance/complete_item_system_20260715.png`。
- 完整说明见`docs/COMPLETE-ITEM-SYSTEM-1_完整物品与地面掉落外观报告.md`，机器验收见`outputs/validation/complete_item_system_acceptance.json`。
# 2026-07-16：UI-GOTHIC-PREVIEW-1 暗黑哥特统一界面审图样板

- 完成人物选择+创建合并界面、游戏内退出菜单、游戏 HUD 三张隔离式视觉样板；创建人物明确提供战士、法师、道士三职业。
- 根据用户新增规范，HUD不显示人物横向血条/蓝条，改为《Diablo 1》式左红血球、右蓝球；顶部保留怪物目标血条。
- 新大厅背景归档为 `project.generated.ui_preview`；快捷栏使用主资料的药品和卷轴真实图标，未跨级随意引用客户端素材。
- 三张截图均由 Godot 4.7 非 headless OpenGL 真实渲染，分辨率 1280×720；专项隔离测试通过。
- 本轮仅供审图，正式入口、人物选择逻辑、系统菜单和 HUD 运行脚本均未替换，APK未打包。报告：`docs/UI-GOTHIC-PREVIEW-1_暗黑哥特界面审图样板.md`。
- 用户审图修订：删除HUD物品快捷栏和“当前追踪”；血/蓝瓶改为背包自动使用。战士六技能改为每行最多4个的自动施放开关；法师/道士预留“普通攻击+技能”互斥选择，右下动作分别为战士“攻击”和法道“释放技能”。保留切换敌人与自动锁定开关。
- 战士技能语义校正：基本剑术、攻杀剑术不提供开关，作为不可点击的常驻被动显示；仅刺杀、半月、野蛮、烈火保留自动使用开关。
- HUD位置修订：删除左上人物名称/等级/职业卡片；自动锁定归入屏幕右侧固定功能栏；切换敌人移动到攻击键旁，右侧保留后续功能扩展空间。
- 战士技能二次语义修订：基本剑术、攻杀剑术从HUD完全隐藏；刺杀、半月、烈火保留自动开关。野蛮冲撞改为与“攻击”互斥选择，选野蛮后可再次选攻击恢复普通攻击。
- 战士技能最终审图修订：取消野蛮冲撞与攻击互斥；野蛮冲撞移到攻击键旁，按一次立即使用。下方技能区只保留刺杀、半月、烈火三个自动开关。
- 战斗物品栏审图修订：重新开放4格物品栏，默认展示金创药、魔法药、回城卷、随机传送卷；轻触使用、长按0.55秒弹出选物菜单。技能开关整体移到物品栏上方。
- 技能快捷键与技能页面审图：攻击键周围增加3个技能按钮；新增完整技能资料页面，长按技能后选择放入按钮1/2/3。战士保留三个自动开关，法师/道士隐藏开关层，仅使用三个技能按钮选择施法技能。
- HUD交互键审图修订：右下技能圈外、屏幕最右侧增加独立“交互”按钮；该按钮只执行最近目标交互，不占3个技能槽，也不复用任何战斗控制。
- HUD底部与美术修订：生命球、魔法球收拢到中央技能/物品控制台左右两侧；右下攻击、三技能、切敌、交互按钮重新拉开且热区不重叠；中央控制台增加细铜金双框与克制暗红菱形，确立“清爽远古哥特”规范。
- Godot卡死核查：Windows应用日志、Godot运行日志和残留进程均无崩溃证据；确认此前一次挂起由审图命令漏传`UI_PREVIEW_CAPTURE=1`造成。新增`tools/capture_ui_preview.ps1`，提供20秒超时、失败日志和进程树自动清理。
- HUD图标来源纠偏：生成的哥特图标只保留给普通攻击与非技能功能；刺杀、半月、烈火改为直接读取主客户端`Magic.wil`生成的正式运行帧，野蛮冲撞读取真实游戏冲锋验收帧。非按键面板底色同步降低不透明度，执行半透明烟黑+细铜金框规范。
- HUD透明化与技能图标实图验收：新增`ui_clean_world_capture`从当前正式场景隐藏旧HUD后截取纯游戏画面，审图底图改用该真实场景；上下烟黑层降为低透明度。刺杀、半月读取正式效果图集，烈火、野蛮读取对应实际游戏验收近景；普通攻击、交互、切敌、自动锁定读取生成的非技能功能图集。Godot真实OpenGL截图与专项测试通过，色键中间图已删除。
- 防卡死工具增强：`capture_ui_preview.ps1`现会实时读取stderr，发现`SCRIPT ERROR`、解析错误或断言失败后立即清理本次Godot进程树，不再等待完整超时时间。
- HUD全透明与哥特美术收口：删除所有非交互面板烟黑填充，背景透明度统一为0；中央控制台缩至500×204，仅技能格、物品格、功能按钮和资源球保留实体底板。新增透明铜金双线、角括饰、黑铁矛翼、球体金属夹具及暗红宝石节点；Godot 4.7真实OpenGL渲染与专项测试均通过，最新审图为`outputs/visual_acceptance/ui_gothic_preview/in_game_hud.png`。
- 卡死复核：本轮启动前无Godot残留进程，日志无崩溃、脚本错误或导入错误；`capture_ui_preview.ps1 -Mode hud`再次返回`UI_GOTHIC_PREVIEW_CAPTURE_PASS`、`UI_GOTHIC_PREVIEW_TEST_PASS`和`UI_PREVIEW_TOOL_PASS`，无需重启计算机。
- HUD组合美术二次收口：删除中央金色大外框及资源球附近旧菱形夹块；使用生成的黑铁/旧金组合骨架包住左右红蓝球，并以开放桥梁连接中部。技能开关、物品格和右下技能位统一改为透明中心哥特九宫格；框体固定、技能纹理可动态替换。新增`tools/build_gothic_hud_runtime_assets.py`，生成源图移入`dev_art_sources/ui/gothic_hud_v1`，运行目录只保留119KB组合骨架和12KB插槽框。
- Godot导入防伪：首次新增PNG未进入文件系统缓存时出现`No loader found for resource`，该次截图立即作废；随后显式执行Godot`--import`并重跑。`capture_ui_preview.ps1`现将任何`ERROR:`和资源加载器缺失视为失败。最终截图、动态换技能图层测试和三项验收全部PASS，Godot残留进程为0。

## 2026-07-16：HUD V2 图框几何对齐与图标分层修复

- 建立独立OpenCV环境`C:/Users/Administrator/.cache/my_brush_game/ui_geometry_venv`，使用`tools/ui_frame_geometry_opencv.py`扫描组合骨架、方形物品框与三套圆形按钮框的透明孔圆心、半径、边界框和圆度；不再靠截图目测偏移。
- 圆形按钮框选用候选B，生成128×128运行框；生命/魔法球由组合骨架实测孔位动态计算，圆心和半径与美术空白区重合。
- 四个战斗物品格改为74×74正方形，物品图标按方框实测透明孔缩放，编号与数量文字独立置顶。
- 刺杀、半月、烈火、野蛮及攻击/空槽图标改为“内容层→装饰框层→文字层”；每个按钮只保留一个动态图标节点，换技能只替换内容，不替换图框。
- 根据用户复审撤销去背景和抠图方案。当前图标保持原始完整背景，以cover方式覆盖圆孔空白区，由顶层装饰框压边；来源和派生哈希写入`runtime_icon_manifest.json`。
- Godot显式重新导入后，真实OpenGL HUD截图、几何硬断言、方格断言、单图层断言、动态换图断言全部通过；未接入正式运行UI，未打包APK，继续等待用户审图。
