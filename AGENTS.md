# 项目协作与工作树规则

本项目使用一个集成分支和五个专业工作树。开始工作前必须运行 `git branch --show-current`，根据当前分支遵守下面的文件所有权。

- 快速启动时，在完整读取本文件后读取 `docs/CODEX_CONTEXT_SNAPSHOT.md`，先使用其中的基线、工作树状态和最小必读索引，禁止无目的重复扫描全项目或重复运行已通过测试。该快照仅用于导航；任何将被修改、合并、构建或删除的对象仍须用当前 Git、文件内容和专项测试核实。每次重要集成里程碑后由 `codex/integration` 更新快照。
- Godot 自动化测试必须优先通过 `tools/run_godot_tests.ps1` 运行；不得启动 `Godot_v4.7-stable_win64.exe` GUI 版，也不得在受控环境中直接启动未指定项目内日志/用户数据目录的 Godot。该入口固定使用 console/headless、项目内 `outputs/test_logs` 与当前工作树 `.godot/runtime_appdata`，用于避免 Godot 4.7 在 `%APPDATA%` 日志写入被拒绝后触发 `c0000005` 退出崩溃弹窗。

## 分支职责

### `codex/integration`

- 当前主目录，负责基线、跨系统接口、合并、冲突处理和完整验收。
- 独占跨系统文件：`project.godot`、`AGENTS.md`、`scripts/game_root.gd`、`scripts/game_data.gd`、`scripts/region_content.gd`、存档格式、全局服务注册和跨系统测试入口。
- 地图刷新怪物、怪物掉落装备、任务引用地图/怪物/装备等映射，只能在这里最终接入。

### `codex/ui-art`

- 负责 `assets/ui/**`、`scripts/hud.gd`、`scripts/*_panel.gd`、`scripts/equipment_character_preview.gd`、UI 专属素材和 UI 测试。
- 只读取玩法数据；不得修改装备属性、怪物数值、地图内容、掉落和存档格式。
- 公共 UI 视觉规范必须集中复用，禁止每个面板自行复制一套主题。

### `codex/maps`

- 负责 `assets/art/maps/**`、`assets/maps/**`、地图资源目录、`map_editor_workspace/**`、`scripts/map_*.gd`、`scripts/map_assets/**`、`scripts/map_editor/**`、环境目录/验证器，以及地图相关工具和测试。
- 地图只定义位置、碰撞、门点、区域和 `spawn_group_id`；不得直接修改怪物属性或装备掉落。

### `codex/monsters`

- 负责 `assets/art/monsters/**`、怪物/Boss 专属数据、`scripts/enemy.gd`、`scripts/monster_visual.gd`、怪物动画策略、Boss 机制，以及怪物相关工具和测试。
- 使用稳定的 `monster_id` 输出外观、动画、AI 和战斗行为；不得修改地图几何、装备定义或 UI。

### `codex/equipment`

- 负责 `assets/art/items/**`、物品/装备专属数据、`scripts/equipment_rules.gd`、装备美术构建工具和装备相关测试。
- 使用稳定的 `item_id` 输出图像、属性、穿戴和耐久规则；不得修改背包布局、地图内容或怪物刷新。

### `codex/professions-skills`

- 负责职业成长、玩家技能、技能投射物、召唤物、职业战斗公式、技能状态机、技能特效，以及对应数据、构建工具和测试。
- 主要所有权包括 `scripts/profession_rules.gd`、`scripts/skill_projectile.gd`、`scripts/summon_actor.gd`、`scripts/warrior_combat_math.gd`、`assets/data/vanilla_176/skills.json`、`assets/data/vanilla_176/profession_growth.json` 和职业/技能专项测试。
- 玩家职业技能特效可写入 `assets/art/characters/**/effects/**`；`paper_doll`、`wear` 和装备图像仍归装备分支。
- `scripts/skill_panel.gd` 和 `scripts/profession_panel.gd` 的视觉布局仍归 UI 分支；本分支只提供稳定数据与行为接口。
- `scripts/layers/runtime/combat_runtime_service.gd` 属于跨怪物/职业共享运行时，最终修改由集成分支接入。
- 不得修改怪物 AI、地图刷新、装备定义、UI 布局或全局存档格式。

## 品牌与命名规则

- 正式游戏品牌和安装后的应用名称统一为 `HardCore`。
- Android 玩家可见应用名称统一为 `HardCore`；内部包 ID `com.personal.mafaoffline` 为旧安装与存档升级兼容标识，不得仅因品牌改名而变更。
- 玩家可见标题、按钮、世界总称、通用提示和新建内容不得再使用“玛法”“传奇”“MafaOffline”等旧品牌名称。
- 原始客户端/服务端来源说明、历史验收记录、`legend176_data.json` 等内部溯源路径，以及 `mafa_world` 等既有稳定 ID 允许保留；它们不得直接作为玩家可见文案，也不得仅为改名而破坏兼容性。
- 各专业分支新增 UI 文案、数据展示名或导出配置时，必须以 `HardCore` 为品牌基线。

## 数据库与资料源分级硬规则

- `assets/data/source_priority_policy.json` 是全项目数据库、源码、客户端资源和派生资产的唯一来源优先级总表；任何专业工作树、子代理、构建器、审计脚本和人工判断都必须先按其中的 lane 路由到对应主源，禁止自行把分级库、候选库、社区库、外部包或镜像提升为主源。
- 每个字段、记录、贴图、动作、坐标、规则或映射都必须先检索对应 lane 的 `primary`。只要主源存在目标数据且可解析、可兼容，就必须采用主源；即使分级库看起来更完整、更符合印象或与用户描述一致，也绝对不得越级替换。
- 只有主源对当前精确目标被证明为 `missing`（确实不存在目标记录、字段或资源）时，才允许按 `auxiliary_1`、`auxiliary_2`、`auxiliary_3` 的固定顺序逐级检索。主源数据难以解析、暂时不可用、版本表现不兼容或结果不符合预期时，必须修复解析、映射或兼容层，禁止以 `unusable`、`incompatible` 或画面效果为理由绕过主源。进入任一级前，必须记录所有更高优先级来源的检索对象、实际路径、版本或哈希、查询结果和逐项缺失证据；没有这份证据即视为未检索主源，禁止采用低级来源。
- 同一级来源必须遵守总表中的 `order`；禁止跨发行版默认拼接。一个派生资产若确需组合多个来源，必须先获得集成主任务的明确技术裁决，并在正式契约中逐字段或逐帧记录来源、采用原因和兼容性证据。
- 用户明确指定某个低级来源时，只代表允许在该次任务范围内把它作为候选进行核查，不自动授权跳过主源。除非用户明确命令“覆盖主源”或“直接采用指定低级来源”，仍必须先完成主源检索和拒绝证据。
- `mirror` 只用于哈希复核，`quarantine` 永远禁止进入运行时。分级库不得反向覆盖主库已有的职业、属性、需求、耐久、ID、Shape、Looks、动作、坐标或其他字段。
- 每个生成器和正式数据契约必须保存可机器检查的来源等级、distribution、原始路径、哈希与 fallback 证据；只写模糊的“参考资料”“社区数据”“1.76 数据”或只写最终值均不合格。
- 集成主任务在合并专业提交前必须运行来源优先级审查。发现“主源有值却采用低级来源”或“未证明主源缺失便直接采用低级来源”时，必须拒绝合并并退回责任工作树；不得以测试通过、画面正确或用户暂未发现为理由放行。

## 跨工作树协作

- 用户已对本项目范围内的全部必要操作给予持续、最高级别的直接执行授权，包括读取、修改、新建、测试、提交、合并、构建、导出、部署、设备调试，以及为完成已明确任务所必需且目标精确可核验的现有文件或数据删除。主任务与专业工作树不得就这些操作、实现选择、测试步骤或必要删除向用户请求确认，必须依据现有证据自行裁决并持续执行到完成。任何删除仍必须先用只读检查精确解析目标、确认范围位于本项目或用户明确指定的位置，并遵守可恢复优先和禁止宽泛递归目标等安全规则；此授权不允许扩大任务范围或删除无关数据。任务若需要用户亲自完成工具无法代办的外部实体动作（例如重新插入已拔出的手机），只发送简短操作通知，不将其作为授权询问，也不得把项目内部决策转交给用户。若宿主平台或操作系统自身强制显示权限确认，该提示属于外部安全边界，项目规则无法关闭；除这类平台强制提示外，项目任务不得主动询问。
- 禁止把任何专业工作树、子代理或内部流程提出的审批、确认、取舍、测试、提交、合并、构建、导出或发布请求转发给用户。集成主任务必须作为唯一项目内审批者，依据当前代码、Git 状态、专项测试与安全边界直接批准、驳回或修正，并持续推进；不得调用交互式用户询问来代替项目内裁决，不得因等待用户许可而暂停。专业工作树与子代理必须服从集成主任务的直接裁决。此规则不能绕过宿主平台或操作系统强制的外部安全确认，但主任务不得额外添加询问。
- 集成主任务是项目管理者与最终集成者，职责是拆分任务、分配专业工作树、裁决稳定接口、审查提交、逐项合并和完整验收；除 `codex/integration` 独占文件及专业分支明确交付的跨系统接线外，主任务不得代替专业工作树逐文件实现。
- 凡能归属 UI、地图、怪物、装备、职业技能任一专业领域的任务，必须优先直接交给对应永久工作树执行、测试并提交；集成主任务不得为了省去分配步骤而在主树重复实现。
- 跨多个专业领域的问题必须并行拆给各责任工作树。集成主任务收齐交付后，一次只审查和合并一个专业提交；专业提交未通过自身专项测试时不得进入集成。
- 只有跨系统接口、全局运行时编排、存档格式、全局服务注册、专业提交落地所需的最小接线和最终跨域回归，才由集成主任务直接实施。
- 需要修改其他分支所有权内的文件时，不得直接修改；在交付说明中记录所需接口、字段/ID、原因和验收方式。
- 大型原始素材目录 `dev_art_sources`、本地 Godot 工具和 DepotDownloader 不进入 Git；它们由各工作树中的本地目录联接共享，并视为只读输入。
- `.godot` 和 `outputs` 在每个工作树中独立生成，禁止跨工作树共享缓存或输出目录。
- 每个专业分支只提交本领域文件。提交前运行相关专项测试并记录结果。
- 集成分支一次只合并一个专业提交；每次合并后运行冒烟测试，再合并下一个。
- 禁止破坏既有数据可追溯、玩法可扩展、资源可替换、系统可测试的原则。

## 交付格式

每个工作树完成任务时必须给出：

1. 修改文件列表。
2. 测试命令与结果。
3. 新增或变更的稳定 ID。
4. 需要集成分支处理的跨系统接入事项。
5. 当前提交哈希。
